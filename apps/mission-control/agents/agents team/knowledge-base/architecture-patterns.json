{
  "_description": "Reusable patterns and anti-patterns — maintained by Architect-Agent and Backend-Agent",
  "_updated": "2026-03-27",
  "patterns": [
    {
      "id": "PAT-001",
      "name": "API proxy with cached fallback",
      "context": "All Agent Hub API calls from Mission Control frontend",
      "pattern": "Try agentHubFetch() → on failure, return cached data from lib/agent-hub-cache.ts",
      "code_reference": "lib/agent-hub-client.ts, app/api/agent-hub/*/route.ts",
      "when_to_use": "Any new API route that proxies to Agent Hub"
    },
    {
      "id": "PAT-002",
      "name": "File-based storage with ensureDataDir",
      "context": "Local persistence for logs, config, prompt overrides",
      "pattern": "ensureDataDir() → fs.readFile/writeFile in data/ directory",
      "code_reference": "lib/logs-storage.ts, lib/jira/config.ts",
      "when_to_use": "Any new local data that needs to persist across restarts"
    },
    {
      "id": "PAT-003",
      "name": "Dual-source streaming (Agent Hub + OpenAI)",
      "context": "Chat needs resilience when Agent Hub is down",
      "pattern": "Try Agent Hub executeAgent() → on failure, fall back to OpenAI with cached system prompt",
      "code_reference": "app/api/chat/route.ts",
      "when_to_use": "Only for chat endpoint. Do not duplicate this pattern elsewhere."
    },
    {
      "id": "PAT-004",
      "name": "SWR hooks with revalidateOnFocus: false",
      "context": "All client-side data fetching",
      "pattern": "useSWR(url, fetcher, { revalidateOnFocus: false })",
      "code_reference": "lib/hooks/use-agents.ts",
      "when_to_use": "Any new client-side data fetching hook"
    },
    {
      "id": "PAT-005",
      "name": "singleFlight request deduplication",
      "context": "SB frontend makes duplicate API calls from multiple components rendering simultaneously",
      "pattern": "Wrap fetch in singleFlight(key, fetchFn) — if same key is in-flight, return existing promise instead of new request",
      "code_reference": "sb/sb-chat-ui/src/utils/singleFlight.ts",
      "when_to_use": "Any data fetching that multiple components trigger simultaneously",
      "source_project": "sb"
    },
    {
      "id": "PAT-006",
      "name": "Metadata-driven integration framework",
      "context": "SB supports 27 integrations (JIRA, Linear, SendGrid, ElevenLabs, etc.) that need discoverable actions",
      "pattern": "Each integration exports action metadata (id, name, description, parameters schema). /integrations/discover returns all. AI assistants use as function calling tools.",
      "code_reference": "sb/sb-api-services-v2/src/integrations/",
      "when_to_use": "When adding new external service integrations to agent capabilities",
      "source_project": "sb"
    },
    {
      "id": "PAT-007",
      "name": "Prompt versioning with AI-generated change descriptions",
      "context": "SB tracks every system prompt change for auditability",
      "pattern": "On assistant update, save new PromptHistory record with version++. Use GPT-4o-mini to generate human-readable change description comparing old vs new prompt.",
      "code_reference": "sb/sb-api-services-v2/src/services/prompt-history.service.ts",
      "when_to_use": "Any system that manages agent prompts and needs audit trail",
      "source_project": "sb"
    },
    {
      "id": "PAT-008",
      "name": "Scope-based file management with TTL",
      "context": "SB handles file uploads across different lifecycle scopes",
      "pattern": "Files tagged with scope (temporary/session/agent/team/company). Temporary files auto-deleted after TTL. Multi-backend: memory, disk, GCP, S3.",
      "code_reference": "sb/sb-api-services-v2/src/services/unified-workspace.service.ts",
      "when_to_use": "Any file upload system that needs lifecycle management",
      "source_project": "sb"
    },
    {
      "id": "PAT-009",
      "name": "Research-Agent как первый фильтр: корректный отказ от нерелевантной задачи",
      "context": "В шаге s0-research агент получил техническое задание на разработку UI (\"Добавить кнопку выхода в header\"), корректно идентифицировал несоответствие своему профилю и отказался выполнять задачу, вернув структурированный ответ с объяснением. Оркестратор (s1) подхватил управление и перенаправил задачу по правильному маршруту.",
      "pattern": "Паттерн работает: агент-фильтр на входе пайплайна повышает надёжность маршрутизации. Стоит закрепить в конфигурации пайплайна явный шаг \"pre-routing check\" с возможностью skip для технических задач, чтобы не тратить 15.5s на очевидный bypass.",
      "code_reference": "",
      "when_to_use": "Пайплайн имеет фиксированный первый шаг (research), который не всегда релевантен для задач типа \"добавить UI-элемент\". Агент не был заблокирован — он отработал роль \"умного фильтра\".",
      "source_pipeline": "9ea3ecdf-b534-454c-82d7-2ddbd1b4f75d"
    },
    {
      "id": "PAT-010",
      "name": "Параллельный запуск Architect + Cyber на одном шаге (s3/s3.5)",
      "context": "Архитектурное решение (s3-architect, 73.8s) и threat model (s3.5-cyber, 66.2s) выполнялись как параллельные шаги одного уровня. Cyber-agent получил на вход архитектуру и выдал threat model до того, как frontend начал реализацию. Это обеспечило security-by-design: фронтенд (s4) получил оба артефакта одновременно.",
      "pattern": "Паттерн \"Architect ∥ Cyber → Frontend\" следует зафиксировать как обязательный для всех фич, затрагивающих auth, сессии, платежи или персональные данные. Экономия времени по сравнению с последовательным запуском: ~66s.",
      "code_reference": "",
      "when_to_use": "Сознательное архитектурное решение: безопасность не добавляется постфактум, а встраивается в процесс параллельно с проектированием.",
      "source_pipeline": "9ea3ecdf-b534-454c-82d7-2ddbd1b4f75d"
    },
    {
      "id": "PAT-011",
      "name": "Single-agent UI pipeline достаточен для изолированных React-компонентов",
      "context": "Задача на создание PipelineStatusBadge (TypeScript + Tailwind + Lucide) была выполнена одним агентом (s4-frontend) за 17.5s с оценкой 10/10 без ретраев и эскалаций. Архитектура pipeline не требовала подключения architect-agent, qa-agent или cyber-agent.",
      "pattern": "Для чисто фронтендовых компонентов без API-интеграции, state management или security-поверхности — маршрутизация только через frontend-agent является оптимальной стратегией. Не добавляй агентов ради полноты pipeline, если домен задачи однозначен.",
      "code_reference": "",
      "when_to_use": "N/A — успешный паттерн",
      "source_pipeline": "611a82c1-ebf4-4da7-af53-f1dff59b6e48"
    },
    {
      "id": "PAT-012",
      "name": "Single-step frontend pipeline sufficient for self-contained UI components",
      "context": "The full pipeline for PipelineStatusBadge was completed by a single agent (s4-frontend) in 30.4s with a perfect score of 10/10 (C:10, S:10, A:10), with no retries, no escalations, and no handoffs to other agents (architect, qa, cyber, devops).",
      "pattern": "For purely presentational, self-contained React components with explicit tech constraints given in the prompt, route directly to frontend-agent only. Skip architect-agent (no structural decisions), qa-agent (no logic branches or edge-case-heavy state), and cyber-agent (no public surface, no sensitive data). This reduces pipeline overhead by ~70% and total duration from estimated 90–120s to under 35s.",
      "code_reference": "",
      "when_to_use": "N/A — success pattern. The task was narrowly scoped: a stateless presentational component with no API calls, no auth, no DB interaction, and no cross-cutting concerns. All required context (Tailwind, Lucide, status enum, animation requirement) was provided upfront in the task brief.",
      "source_pipeline": "6bf5f54f-7ea2-4548-83b0-e1f884690dcb"
    },
    {
      "id": "PAT-013",
      "name": "Client-Side Store Snapshot as API Data Source Requires Architectural Guardrails",
      "context": "The core design — GET /api/orchestration/export reading execution history from a Zustand store snapshot — was flagged by the PM (s2-pm) as architecturally questionable. Zustand is a client-side state manager; a server-side API endpoint cannot natively read it. The architect (s3-architect) correctly identified this as the key decision point requiring resolution before implementation.",
      "pattern": "Establish canonical pattern for this codebase: Zustand store is ephemeral client state only. For any data that must be served via API, it must first be persisted to a server-side store (DB or file). Export endpoint should read from server-side persistence layer, not attempt to access client store. Document this in `knowledge-base/tech-decisions.json` as a standing architectural constraint.",
      "code_reference": "",
      "when_to_use": "The original task brief conflated client-side state (Zustand) with server-side persistence. The API endpoint would need either: (a) the client to POST the snapshot to the server first, or (b) the history to be persisted server-side independently of Zustand.",
      "source_pipeline": "f8650362-296e-490c-863b-a011570e589c"
    },
    {
      "id": "PAT-014",
      "name": "Orchestrator как единственный quality gate создаёт single point of failure для pipeline",
      "context": "s1-orchestrator получил 10/10 и сгенерировал детальный уточняющий brief. Однако весь pipeline завис после s2-pm abort — нет механизма partial recovery или checkpoint restart. Все 9 downstream шагов остались в pending без возможности возобновить с точки отказа.",
      "pattern": "Реализовать checkpoint-based restart: сохранять output каждого completed шага в `pipeline-state/{run-id}/{step-id}.json`. При retry запускать pipeline с последнего успешного checkpoint, не с начала. Для данного pipeline: s0 и s1 уже completed — повторный запуск должен стартовать с s2-pm, передавая сохранённые outputs.",
      "code_reference": "",
      "when_to_use": "Pipeline реализован как линейная цепочка без checkpoint persistence. При abort любого шага весь прогресс теряется. Нет разделения между \"блокирующими\" и \"не блокирующими\" шагами.",
      "source_pipeline": "6bd3e517-9162-4258-a467-d0a687a3e550"
    },
    {
      "id": "PAT-015",
      "name": "Agent Self-Awareness as a Routing Signal",
      "context": "Research-Agent demonstrated useful meta-cognitive behavior: it recognized it was the wrong agent for the task and articulated why, naming the correct domain (backend). This is a positive emergent behavior worth formalizing.",
      "pattern": "Formalize \"domain rejection with redirect\" as a first-class agent response type in the agent contract spec. Structure it as: `{ type: \"scope_rejection\", reason: string, suggested_agent: string, confidence: 0-1 }`. Orchestrator should treat `suggested_agent` as a high-confidence routing hint and auto-redirect without retry. Log these redirects to `knowledge-base/routing-corrections.json` to train the pre-routing classifier over time.",
      "code_reference": "",
      "when_to_use": "N/A — this is a pattern to preserve, not a failure.",
      "source_pipeline": "374d41a7-ca0a-4244-8142-b3e60898ef4f"
    },
    {
      "id": "PAT-016",
      "name": "Research-Agent as Accurate Scope Classifier Prevents Wasted Downstream Work",
      "context": "Research-Agent (s0) correctly assessed the task scope in 11.9s, scored 8.9/10, and produced a clear redirect signal: \"no research needed, this is a frontend task.\" This is the correct behavior — the agent functioned as a lightweight classifier before expensive agents (Architect, Backend, Cyber) were invoked.",
      "pattern": "Formalize the Research-Agent's scope classification output as a structured signal (e.g., `{ research_needed: false, recommended_agents: [\"frontend-agent\"], skip_agents: [\"architect-agent\", \"backend-agent\", \"cyber-agent\"] }`). The Orchestrator should consume this signal to prune the pipeline dynamically. This transforms a narrative redirect into a machine-readable routing directive, making the pattern reliable rather than dependent on Orchestrator interpretation.",
      "code_reference": "",
      "when_to_use": "N/A — this is a positive pattern.",
      "source_pipeline": "29d1d8c5-fc4c-425d-86bc-530656300a5b"
    },
    {
      "id": "PAT-017",
      "name": "Clarification-First Gate Before Delegation",
      "context": "When s1-orchestrator received an ambiguous task (\"orchistration test\"), instead of attempting to delegate to downstream agents or making assumptions, it generated 7 targeted clarifying questions to scope the work before any pipeline resources were consumed. Downstream steps (s2–s6) were skipped entirely.",
      "pattern": "Maintain this pattern: Orchestrator should always block pipeline progression when task intent is undefined. Clarification cost (24.7s) is far cheaper than running 6+ agents on a misunderstood brief. Formalize this as a quality gate: if task description < threshold specificity, emit clarification block instead of TASK BRIEF.",
      "code_reference": "",
      "when_to_use": "N/A — this is intentional design behavior",
      "source_pipeline": "823256db-52b3-4914-96fa-e78ca14c2497"
    },
    {
      "id": "PAT-018",
      "name": "Mobile-First Tailwind CSS Strategy Adopted for Responsive Shell",
      "context": "ADR-007 was created and accepted in s3-architect (44s, 10/10) establishing Tailwind CSS responsive utilities with mobile-first breakpoints as the canonical approach for the responsive shell. This decision was made early and cleanly, enabling downstream agents (frontend, designer) to work without ambiguity.",
      "pattern": "Continue front-loading architecture decisions (ADR format) before frontend/designer work begins. Mobile-first constraint eliminates a whole class of responsive rework later.",
      "code_reference": "",
      "when_to_use": "N/A — positive pattern",
      "source_pipeline": "fe6bc195-9ab2-4591-8ead-db3b5233bc5c"
    },
    {
      "id": "PAT-019",
      "name": "Sequential PM → Architect → Frontend → Designer → QA Pipeline Worked Without Retries",
      "context": "All five steps completed without retries. Total duration 351s for a full UX/UI improvement pipeline covering PRD, ADR, implementation, design specs, and QA. No escalations occurred.",
      "pattern": "This agent ordering (PM defines scope → Architect locks decisions → Frontend implements → Designer specifies → QA validates) is a proven sequence for UI-layer tasks. Note that Designer ran after Frontend (s4-designer after s4-frontend), which is atypical — normally design precedes implementation. This worked here likely because the task was iterative improvement, not greenfield. Flag this ordering as context-dependent: safe for improvement tasks, risky for new feature builds.",
      "code_reference": "",
      "when_to_use": "N/A — positive pattern",
      "source_pipeline": "fe6bc195-9ab2-4591-8ead-db3b5233bc5c"
    },
    {
      "id": "PAT-020",
      "name": "Compliance-Isolated Database Connection Pattern for Sensitive Loyalty Domain",
      "context": "s3.4-fileplan and s3.1-adr both specified that the loyalty domain should use a dedicated PostgreSQL client instance (`lib/loyalty/db.ts`) isolated from the general CRM connection pool, explicitly citing 152-FZ (personal data) and 54-FZ (fiscal compliance) requirements. This pattern was consistently applied across the file plan (10 service files all importing from the isolated client) and the ERD (loyalty tables designed as a self-contained domain with UUID foreign keys to the main schema rather than direct joins).",
      "pattern": "Formalize this as a reusable architectural template for any future domain handling personal or fiscal data: isolated DB client instance, domain-scoped service layer, no cross-domain ORM joins, explicit compliance annotation in the ADR. The pattern should be documented in the project's architecture decision registry as a standing rule, not rediscovered per feature.",
      "code_reference": "",
      "when_to_use": "N/A — this is a deliberate positive pattern.",
      "source_pipeline": "1a3f1cdb-53f9-45d7-92ea-0925506658db"
    },
    {
      "id": "PAT-021",
      "name": "Fileplan as Contract Enables Cross-Agent Artifact Accountability",
      "context": "s3.4-fileplan produced an explicit enumeration of files to create/modify before any implementation began. This artifact served as the ground truth that allowed s8-technical-qa to immediately identify the missing route file by name — the QA agent didn't have to infer what *should* exist; it had a declared contract to validate against.",
      "pattern": "Preserve and enforce the fileplan-as-contract pattern in all pipelines. The fileplan output should be passed explicitly as a structured input to QA agents (not just available in context) so they can perform systematic file-by-file existence and content checks as their first action, before any behavioral or business logic validation.",
      "code_reference": "",
      "when_to_use": "N/A — this is a positive pattern.",
      "source_pipeline": "4fe80099-98e7-4ed2-8391-1c0c932f16c4"
    },
    {
      "id": "PAT-022",
      "name": "Fix-Loop Pattern: Inline Remediation Cycles (s5-backend-fix-N + s5-qa-reval-N) Contained Blast Radius",
      "context": "When s8-technical-qa detected the missing file, the pipeline correctly spawned a targeted fix loop (fix-1 → reval-1 → fix-2 → reval-2) rather than restarting the full pipeline or escalating to a human. Each iteration was short (9.9–16.8s) and scoped exclusively to the broken backend artifact, leaving all upstream design artifacts (ADR, API contracts, ERD, fileplan) untouched.",
      "pattern": "Formalize this as a reusable pipeline primitive: `fix-loop(agent, qa, max_iterations=3)`. The loop should: (1) pass the QA failure diff as explicit context to the fix agent, (2) run a lightweight re-validation after each fix, (3) hard-stop and escalate if max_iterations is reached without resolution. The current execution resolved in 2 iterations, which is within an acceptable budget. Codify the pattern so it applies consistently across backend, frontend, and infra agents.",
      "code_reference": "",
      "when_to_use": "N/A — this is a positive pattern.",
      "source_pipeline": "4fe80099-98e7-4ed2-8391-1c0c932f16c4"
    },
    {
      "id": "PAT-023",
      "name": "Prior Failed Run as Implicit Test Oracle for Targeted Fixes",
      "context": "The pipeline completed in 29s with perfect scores (10/10 across all dimensions) on a task that had previously failed. s7-frontend's output explicitly references the failed prior attempt, indicating the knowledge base / run history was consulted and acted as a negative example that guided the correct implementation path.",
      "pattern": "Establish a convention: for any UI micro-task (step/increment/format changes), agents should query run history for same-component prior attempts before writing code. This reduces iteration cycles from N retries to 0.",
      "code_reference": "",
      "when_to_use": "N/A — this is a positive pattern, not a failure.",
      "source_pipeline": "50f840fa-feec-4271-8da6-07af9874fa1a"
    },
    {
      "id": "PAT-024",
      "name": "Full Gate Pipeline Overhead for Trivial String Mutations",
      "context": "A single-field string rename (\"Beauty CRM Full Pipeline\" → \"MC pipeline\") was routed through the full 7-step pipeline including PRD gate, Cyber agent, Arch gate, Tech Review, Final Verdict, and Consolidation — totaling 111s of execution time. The s4-cyber agent itself confirmed \"Risk Level: Low\" and the s2.5-prd-gate confirmed it as a \"single-field rename.\"",
      "pattern": "Implement a complexity classifier at s1-orchestrator that can emit a `fast-track` signal for tasks matching patterns like: single-field rename, constant value change, label update. Fast-track should skip s4-cyber, s4.5-arch-gate, s8.5-tech-review, and s11-final-verdict, routing directly to s12b-consolidation. Estimated reduction: ~80s for this class of task.",
      "code_reference": "",
      "when_to_use": "No lightweight routing path exists for tasks that are provably trivial at the orchestrator level (s1). The pipeline lacks a fast-track lane that bypasses heavyweight gates when task complexity is demonstrably below a threshold.",
      "source_pipeline": "4d998ff1-d88b-4416-a98e-9ad7a13ce8fd"
    },
    {
      "id": "PAT-025",
      "name": "Lightweight Cyber Pre-Screening Accelerates Safe UI-Only Tasks",
      "context": "s4-cyber completed in 4.3s and correctly classified the title color change as \"Low\" risk with no auth, data, or logic concerns. This fast gate allowed the pipeline to proceed confidently without blocking the frontend agent on security overhead.",
      "pattern": "Maintain a fast-path cyber pre-screen for cosmetic/frontend-only tasks. When s4-cyber returns Low risk in <5s, downstream agents (s7-frontend, s10-cyber-audit) can execute with reduced scrutiny overhead, potentially allowing pipeline optimization via parallelization or abbreviated audit scope.",
      "code_reference": "",
      "when_to_use": "N/A — positive pattern",
      "source_pipeline": "b158e1d7-f699-4eb0-8578-8c50e81e6856"
    },
    {
      "id": "PAT-026",
      "name": "Full Audit on Trivial Changes Creates Disproportionate Pipeline Cost",
      "context": "s10-cyber-audit spent 46.1s — over 59% of total pipeline duration (77.9s) — auditing a single CSS class change on an h1 element. s4-cyber had already confirmed Low risk in 4.3s. The audit read the full orchestration page file to audit \"real code,\" which is thorough but disproportionate for a cosmetic change pre-cleared by the risk screener.",
      "pattern": "Introduce a risk-gated audit depth rule: when s4-cyber scores Low risk on a pure cosmetic change, s10-cyber-audit should execute a scoped audit (verify only the changed lines/component) rather than a full-file read. This could reduce audit time by an estimated 60–70% for this class of task, bringing total pipeline duration under 30s.",
      "code_reference": "",
      "when_to_use": "The pipeline does not currently differentiate audit depth based on s4-cyber's risk classification. A Low-risk pre-screen does not trigger a lightweight audit path — s10-cyber-audit runs full-depth regardless.",
      "source_pipeline": "b158e1d7-f699-4eb0-8578-8c50e81e6856"
    }
  ],
  "anti_patterns": [
    {
      "id": "ANTI-001",
      "name": "Using npx next instead of local binary",
      "problem": "Root monorepo has next@16.1.1, mission-control needs 15.5.4",
      "solution": "Always use node_modules/.bin/next for build/dev commands"
    },
    {
      "id": "ANTI-002",
      "name": "Hardcoding Jira project key",
      "problem": "Project key may change, breaks automation",
      "solution": "Always use listProjects API or read from jira-config.json defaultProjectKey"
    },
    {
      "id": "ANTI-003",
      "name": "Date.now() or Math.random() in SSR components",
      "problem": "Causes hydration mismatch between server and client",
      "solution": "Use useEffect for dynamic values, or pass as props from server"
    },
    {
      "id": "ANTI-004",
      "name": "Hybrid state management without migration plan",
      "problem": "SB uses both Zustand (12 stores) and MobX State Tree (4 entity stores). Two paradigms increase onboarding time and bug surface.",
      "solution": "Freeze MobX stores — no new features. Migrate entity CRUD to Zustand one model at a time. Start with Team (simplest).",
      "source_project": "sb"
    },
    {
      "id": "ANTI-005",
      "name": "Company isolation via application-layer filter only",
      "problem": "Every MongoDB query must include companyId filter. One missed filter = cross-tenant data leak.",
      "solution": "Add Mongoose middleware (pre-find, pre-findOne) that auto-injects companyId from request context. Defense in depth.",
      "source_project": "sb"
    },
    {
      "id": "ANTI-006",
      "name": "27 integrations without circuit breaker",
      "problem": "One slow/failing integration (e.g., JIRA timeout) blocks the entire request chain.",
      "solution": "Add circuit breaker pattern per integration: after 3 failures in 60s, stop calling for 30s. Return cached/fallback response.",
      "source_project": "sb"
    }
  ]
}