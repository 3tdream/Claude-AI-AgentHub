{
  "_description": "Known issues, root causes, and solutions — maintained by QA-Agent and all agents",
  "_updated": "2026-03-16",
  "patterns": [
    {
      "id": "FAIL-001",
      "category": "build",
      "title": "Next.js version mismatch in monorepo",
      "symptoms": "Build fails with 'Jest worker encountered child process exceptions' or Turbopack internal error",
      "root_cause": "Root monorepo has next@16.1.1, mission-control needs 15.5.4. npx resolves to root version.",
      "solution": "Use node_modules/.bin/next build instead of npx next build",
      "date_discovered": "2026-03-10",
      "recurrence": "Every time someone runs npx next"
    },
    {
      "id": "FAIL-002",
      "category": "hydration",
      "title": "SSR hydration mismatch with dynamic values",
      "symptoms": "React hydration error: server rendered text didn't match client",
      "root_cause": "Using Date.now(), Math.random(), or locale-dependent formatting in SSR components",
      "solution": "Move dynamic values to useEffect or pass stable values from server",
      "date_discovered": "2026-03",
      "recurrence": "When adding date displays or random IDs in page components"
    },
    {
      "id": "FAIL-003",
      "category": "api",
      "title": "Agent Hub connection timeout",
      "symptoms": "Chat returns OpenAI fallback, API routes return cached: true",
      "root_cause": "Agent Hub server on localhost:3000 not running or network timeout (3s limit)",
      "solution": "Start Agent Hub server. Check AGENT_HUB_LIVE env var. Cached data serves as fallback.",
      "date_discovered": "2026-02",
      "recurrence": "After system restart or when Agent Hub is updating"
    },
    {
      "id": "FAIL-004",
      "category": "api",
      "title": "Agent Hub execute returns empty on long analytical prompts",
      "symptoms": "MCP execute call returns empty content for prompts requesting structured analysis (8+ lesson extraction)",
      "root_cause": "Likely token limit or timeout on stateless /execute endpoint. Agent starts tool calls but response exceeds output token limit (4000-6000 tokens).",
      "solution": "Break large prompts into smaller focused queries. Or use session-based chat for long interactions. Orchestrator hello works (short response), analysis fails (long response).",
      "date_discovered": "2026-03-12",
      "recurrence": "When using MCP execute for complex analytical tasks",
      "source_project": "mission-control"
    },
    {
      "id": "FAIL-005",
      "category": "devops",
      "title": "pnpm hard-link corruption on Windows 11",
      "symptoms": "node_modules files show as directories with year 1601 timestamps. npm install fails.",
      "root_cause": "pnpm uses hard-links from global store. Windows NTFS sometimes corrupts these links during forced shutdown or antivirus scan.",
      "solution": "PowerShell Remove-Item -Recurse -Force on node_modules, then npm install --no-workspaces instead of pnpm.",
      "date_discovered": "2026-03-12",
      "recurrence": "On Windows systems using pnpm with hard-links"
    },
    {
      "id": "FAIL-006",
      "category": "build",
      "title": "Шаблонные плейсхолдеры не резолвятся до запуска агента",
      "symptoms": "Шаг s4-frontend получил на вход неразрезолвленные плейсхолдеры (`{{step_s3-architect_output}}`), что привело к тому, что агент не смог выполнить реализацию и запросил недостающие данные — несмотря на итоговую оценку 10/10.",
      "root_cause": "Пайплайн не проверил, что все зависимые шаги (s3-architect) завершились и их output был подставлен в контекст перед запуском s4-frontend. Либо шаг s3-architect был пропущен/не включён в этот прогон, либо механизм передачи контекста между шагами не сработал.",
      "solution": "Добавить pre-flight валидацию перед каждым шагом: проверять, что все плейсхолдеры вида `{{step_*_output}}` заменены реальными данными. Если хотя бы один плейсхолдер не резолвится — блокировать запуск шага и эскалировать к оркестратору с указанием конкретного незавершённого зависимого шага.",
      "date_discovered": "2026-03-13",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "c023cb6d-511f-4074-ae4e-8f56b607e726"
    },
    {
      "id": "FAIL-007",
      "category": "build",
      "title": "Оценка 10/10 при фактически неуспешном выходе агента",
      "symptoms": "Шаг s4-frontend получил оценку качества 10/10, однако его фактический output — запрос недостающих данных, а не реализованная кнопка выхода. Скоринг не отразил реальный результат.",
      "root_cause": "Система оценки качества анализировала структуру и полноту ответа агента (агент корректно описал проблему, указал конкретный плейсхолдер), но не проверяла, выполнена ли целевая задача шага. Completeness/Specificity/Actionability оценивались по качеству текста, а не по факту поставки артефакта.",
      "solution": "В quality gate добавить обязательный критерий **task-completion check**: для шагов с ожидаемым артефактом (код, файл, компонент) оценка не может быть выше 5, если артефакт не поставлен. Разделить метрику \"качество коммуникации агента\" и \"выполнение задачи шага\".",
      "date_discovered": "2026-03-13",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "c023cb6d-511f-4074-ae4e-8f56b607e726"
    },
    {
      "id": "FAIL-008",
      "category": "build",
      "title": "Frontend Agent падает без диагностической информации при простых UI-задачах",
      "symptoms": "Шаг s4-frontend завершился с ошибкой \"Execution failed\" при выполнении тривиальной задачи — добавление кнопки выхода в header. Никакого стек-трейса, кода ошибки или контекста не предоставлено. Quality scores отсутствуют, что означает агент не дошёл до стадии генерации output.",
      "root_cause": "Вероятные причины (в порядке убывания вероятности): 1) Отсутствует контекст — агент не получил путь к файлу header-компонента, текущую структуру проекта или design tokens для кнопки; 2) Timeout на модели без retry-логики; 3) Агент запущен без активной фазы — ACTIVE_PHASE.md не был прочитан перед делегированием, и агент не знал, какой стек используется (Next.js App Router vs Pages Router).",
      "solution": "1) При делегировании frontend-agent ВСЕГДА указывать: точный путь к файлу (`src/components/layout/Header.tsx`), текущий код компонента как INPUT, design tokens для кнопки (цвет, размер, иконка). 2) Добавить в Task Brief явный CONSTRAINTS блок с указанием роутера и версии Next.js. 3) Настроить pipeline так, чтобы при \"Execution failed\" без score логировался последний prompt, отправленный агенту. 4) Для UI-задач такого уровня (< 20 строк кода) — использовать модель с меньшим timeout, не GPT-4 уровня.",
      "date_discovered": "2026-03-13",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "11e62f7d-cba8-4c50-8a5d-3ef11c0efb3f"
    },
    {
      "id": "FAIL-009",
      "category": "architecture",
      "title": "Одношаговый pipeline для UI-задачи без QA и Cyber проверки",
      "symptoms": "Pipeline для задачи \"добавить кнопку выхода\" содержал только один шаг (s4-frontend). Отсутствовали: qa-agent для проверки что logout корректно очищает сессию/токены, cyber-agent для проверки что client-side logout не оставляет чувствительные данные в localStorage/cookies. Кнопка выхода — это auth-related функциональность, которая по правилам Orchestrator требует обязательного участия cyber-agent.",
      "root_cause": "Задача была классифицирована как \"UI task\" (добавить кнопку), а не как \"auth task\" (реализовать logout flow). Это привело к неполному pipeline без обязательных security и quality gates.",
      "solution": "Любая задача содержащая слова \"выход\", \"logout\", \"signout\", \"сессия\", \"токен\" — автоматически триггерит расширенный pipeline: frontend-agent → cyber-agent (проверка: токены очищены, redirect корректен, нет утечки state) → qa-agent (edge cases: двойной клик, logout во время pending request, logout на мобильном). Добавить это правило в routing table knowledge-base.",
      "date_discovered": "2026-03-13",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "11e62f7d-cba8-4c50-8a5d-3ef11c0efb3f"
    },
    {
      "id": "FAIL-010",
      "category": "build",
      "title": "Нулевые Quality Scores как индикатор pre-execution failure",
      "symptoms": "Все quality scores отсутствуют (пустые). Это означает агент упал до того, как сгенерировал какой-либо output — то есть проблема на уровне инициализации агента или парсинга Task Brief, а не на уровне качества генерации.",
      "root_cause": "Когда score = null/empty, стандартная retry-логика (score < 8 → retry with feedback) не срабатывает, потому что нечего оценивать. Pipeline завершается как \"failed\" без попытки retry и без эскалации к пользователю — нарушение правила \"Max 2 retries per agent\".",
      "solution": "В pipeline-движке добавить отдельную ветку: если execution_status = \"failed\" AND scores = empty → это не quality failure, это execution failure → немедленная эскалация к пользователю с диагностическим отчётом (последний prompt, модель, timeout, environment). Не считать как retry попытку.",
      "date_discovered": "2026-03-13",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "11e62f7d-cba8-4c50-8a5d-3ef11c0efb3f"
    },
    {
      "id": "FAIL-011",
      "category": "architecture",
      "title": "Пропуск архитектурного шага ведёт к неполному контексту у Frontend-агента",
      "symptoms": "Шаг архитектуры был пропущен в пайплайне, из-за чего Frontend-агент (s4-frontend) получил неполный контекст. Агент вынужден был описывать, что он может сделать, вместо того чтобы реализовать задачу — фактически заблокировался на запросе недостающей информации.",
      "root_cause": "Пайплайн был запущен без обязательного шага Architect-Agent, который должен предоставлять структуру компонентов, точки монтирования и контракты API. Frontend-агент не может принимать архитектурные решения самостоятельно согласно правилам ролей.",
      "solution": "Для задач типа \"добавить UI-элемент в существующий компонент\" (header, nav, layout) — добавить в пайплайн минимальный шаг architect-agent с единственной целью: указать файл компонента, props и место вставки. Альтернатива — передавать эти данные напрямую в INPUT секции Task Brief, если архитектура уже известна.",
      "date_discovered": "2026-03-13",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "db825bc4-17c1-4c87-8565-a0243c4ca3cb"
    },
    {
      "id": "FAIL-012",
      "category": "build",
      "title": "Задача с неполным контекстом завершается как \"completed\" вместо \"blocked\"",
      "symptoms": "Пайплайн имеет статус `completed`, хотя реальный результат — агент запросил недостающие данные и ничего не реализовал. Кнопка выхода в header не добавлена. Статус вводит в заблуждение.",
      "root_cause": "Система статусов пайплайна отслеживает завершение шага (агент ответил), а не достижение цели шага (задача реализована). Нет разницы между \"агент выполнил задачу\" и \"агент объяснил, почему не может выполнить задачу\".",
      "solution": "Ввести статус `blocked` для шагов, где агент явно указывает на недостаток входных данных. Триггер: если вывод агента содержит паттерны \"нужно от вас\", \"недоступна\", \"пропущен\" — автоматически помечать шаг как `blocked`, не `completed`, и останавливать пайплайн с запросом к пользователю.",
      "date_discovered": "2026-03-13",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "db825bc4-17c1-4c87-8565-a0243c4ca3cb"
    },
    {
      "id": "FAIL-013",
      "category": "build",
      "title": "Frontend-агент падает без диагностируемой ошибки при простых UI-задачах",
      "symptoms": "Шаг s4-frontend завершился с ошибкой \"Execution failed\" при попытке добавить кнопку выхода в header. Задача тривиальная (UI-компонент, ~10-20 строк кода), однако пайплайн упал полностью за 42.5 секунды без retry и без содержательного error message.",
      "root_cause": "Ошибка \"Execution failed\" без stack trace указывает на одну из трёх причин: (1) отсутствие контекста — агент не получил путь к файлу header-компонента и не смог его найти; (2) отсутствие auth-контекста — кнопка logout требует знания о механизме аутентификации (session/JWT/cookie), которое не было передано в task brief; (3) инфраструктурный сбой самого агента (timeout, OOM, недоступность модели).",
      "solution": "1. При делегировании logout-кнопки frontend-агенту обязательно передавать: точный путь к header-компоненту, используемый auth-механизм (signOut из next-auth / clear cookie / etc.), импорты которые нужны. 2. Добавить в task brief секцию CONTEXT с результатом `find . -name \"*header*\" -o -name \"*Header*\"`. 3. Если причина инфраструктурная — добавить retry: 1 для s4-frontend в конфиге пайплайна, т.к. простые UI-задачи не должны падать без retry.",
      "date_discovered": "2026-03-13",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "28c69700-bd93-451c-8535-88f39677a788"
    },
    {
      "id": "FAIL-014",
      "category": "architecture",
      "title": "Задача logout делегирована без передачи auth-контекста",
      "symptoms": "Кнопка выхода — это не чистый UI-компонент: она вызывает функцию завершения сессии, которая зависит от конкретной auth-реализации проекта. Пайплайн направил задачу напрямую в s4-frontend, пропустив проверку knowledge-base и не передав информацию о том, как устроена аутентификация в Beauty CRM.",
      "root_cause": "Отсутствует обязательная проверка `knowledge-base/tech-decisions.json` перед роутингом задач, затрагивающих auth. Это нарушает правило Orchestrator: \"перед роутингом проверить knowledge-base на past decisions\".",
      "solution": "Для любой задачи со словами \"выход\", \"logout\", \"signOut\", \"сессия\" — обязательно: (1) прочитать knowledge-base/tech-decisions.json на предмет записи об auth-провайдере; (2) включить найденное решение в секцию INPUT task brief для frontend-агента; (3) если запись отсутствует — сначала запросить у architect-agent, затем делегировать frontend.",
      "date_discovered": "2026-03-13",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "28c69700-bd93-451c-8535-88f39677a788"
    },
    {
      "id": "FAIL-015",
      "category": "devops",
      "title": "Нулевые quality scores при failed-статусе скрывают реальную причину сбоя",
      "symptoms": "В результатах выполнения поле QUALITY SCORES пустое. При падении агента оценки не выставляются, что делает post-mortem анализ неполным — невозможно определить, упал ли агент до генерации вывода или вывод был сгенерирован, но не прошёл валидацию.",
      "root_cause": "Система quality scoring срабатывает только на успешный вывод агента. Если агент не вернул output (execution error), scoring pipeline не запускается и не логирует даже частичный результат.",
      "solution": "Добавить в pipeline логику: при failed-статусе агента сохранять последний partial output (если есть) и выставлять score 0 с явной причиной \"execution_failed_before_output\". Это позволит отличать \"агент упал при старте\" от \"агент сгенерировал невалидный результат\".",
      "date_discovered": "2026-03-13",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "28c69700-bd93-451c-8535-88f39677a788"
    },
    {
      "id": "FAIL-016",
      "category": "build",
      "title": "PM-агент генерирует PRD без ответов на уточняющие вопросы оркестратора",
      "symptoms": "Оркестратор (s1) сформировал детальный список уточняющих вопросов к команде/заказчику. PM-агент (s2) зафиксировал в своём выводе, что \"ответы от команды не получены\", но всё равно создал полноценный PRD с предположениями. Пайплайн продолжился без подтверждения требований.",
      "root_cause": "Отсутствует обязательный Checkpoint между s1 и s2 для сбора ответов на уточняющие вопросы. PM-агент заполнил пробелы допущениями, что является нормальным поведением при async-работе, но создаёт риск расхождения с реальными требованиями.",
      "solution": "Для задач, где оркестратор генерирует уточняющие вопросы, добавить явный Checkpoint 1.5 (после s1, до s2) с ожиданием подтверждения пользователя. Альтернатива: PM-агент должен явно маркировать каждое допущение тегом `[ASSUMED — требует подтверждения]` и выносить их в отдельный раздел PRD.",
      "date_discovered": "2026-03-13",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "9ea3ecdf-b534-454c-82d7-2ddbd1b4f75d"
    },
    {
      "id": "FAIL-017",
      "category": "build",
      "title": "QA Agent Output Truncation on Multi-Finding Reports",
      "symptoms": "Step s5-qa scored 7/10 (C:5.5) because the output was truncated mid-way through the third finding block (QA-1-003 and beyond were missing). The QA report covered only 2 of N expected findings, making the output incomplete despite high specificity (8.5) and reasonable actionability (7).",
      "root_cause": "QA Agent attempted to enumerate all findings in a single response without chunking. When the report grew beyond the model's effective output window, the response was cut off mid-sentence, silently dropping the remaining findings rather than signaling incompleteness.",
      "solution": "QA Agent should structure output as: (1) findings summary table first with total count declared upfront, (2) then detailed blocks. If count > 5, split into batches with explicit \"Part 1 of N\" headers. Orchestrator should validate: declared finding count matches actual finding blocks before scoring completeness above 7.",
      "date_discovered": "2026-03-14",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "f8650362-296e-490c-863b-a011570e589c"
    },
    {
      "id": "FAIL-018",
      "category": "architecture",
      "title": "Backend and Frontend Agents Proceeding Without Architect Output",
      "symptoms": "Steps s4-backend and s4-frontend both flagged their own architecture assumptions with `[ASSUMED]` markers, indicating they executed without receiving the architect-agent's design. Both agents scored 10/10 despite this gap, meaning quality scoring did not penalize assumption-based work.",
      "root_cause": "The pipeline ran s3-architect and s4-backend/s4-frontend in a configuration where the architect output was either not passed as input context or the parallel track started before architect completion. Agents compensated professionally by marking assumptions, but the dependency chain was not enforced.",
      "solution": "Enforce hard dependency gate: s4-backend and s4-frontend must receive s3-architect output as explicit INPUT field in their Task Brief. Orchestrator should block routing to implementation agents if architect output is absent or scored below 8. Add to routing rules: \"DEPENDS ON: s3-architect PASS\" for all implementation steps.",
      "date_discovered": "2026-03-14",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "f8650362-296e-490c-863b-a011570e589c"
    },
    {
      "id": "FAIL-019",
      "category": "build",
      "title": "Quality Score Inflation When Assumptions Mask Missing Dependencies",
      "symptoms": "s4-backend and s4-frontend both received 10/10 scores despite operating on assumed architecture rather than validated design. The scoring rubric (completeness, specificity, actionability) does not account for dependency fulfillment — an agent can score perfectly while building on unvalidated foundations.",
      "root_cause": "The quality gate scoring criteria evaluate the output in isolation, not relative to whether required inputs were actually provided. A well-written assumption is indistinguishable from a well-written fact under the current rubric.",
      "solution": "Add a fourth scoring dimension: **Dependency Integrity** — were all declared DEPENDS ON inputs actually received and used? If any `[ASSUMED]` markers are present in output, cap Dependency Integrity at 5, which should reduce overall score below 8 and trigger a review flag. This prevents assumption-based work from silently passing quality gates.",
      "date_discovered": "2026-03-14",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "f8650362-296e-490c-863b-a011570e589c"
    },
    {
      "id": "FAIL-020",
      "category": "build",
      "title": "PM-Agent abort без причины при генерации user stories для сложного security-контекста",
      "symptoms": "Шаг s2-pm завершился с ошибкой \"signal is aborted without reason\" после 1 retry, набрав 4.3/10 (C:4.5 S:5 A:3.5). Pipeline остановился полностью — все downstream шаги (architect, cyber, backend, frontend, qa, devops) остались в pending. Задача PM-Agent включала создание user stories для системы с security-требованиями (шифрование ключей, audit log, валидация через внешние API).",
      "root_cause": "Вероятно, PM-Agent получил слишком объёмный контекст из s1-orchestrator (10/10, детальный brief) плюс неполный research из s0 (обрезан на полуслове). Комбинация большого входного контекста + необходимость генерировать acceptance criteria для security-специфичных stories (шифрование, audit log, валидация ключей у трёх провайдеров) могла вызвать timeout или context overflow, приведший к abort сигналу. Низкий actionability score (3.5) указывает, что даже частичный output был непригоден для передачи architect-agent.",
      "solution": "(1) Разбить PM-задачу на два под-шага: сначала базовые CRUD stories (без security), затем отдельный шаг для security/audit stories. (2) Передавать PM-Agent только резюме research (не полный документ), если research > 2000 токенов. (3) Добавить fallback: если PM abort после 1 retry — Orchestrator генерирует минимальный story skeleton самостоятельно и передаёт architect с пометкой \"needs PM refinement post-pipeline\". (4) Установить явный timeout guard на PM-шаге с graceful partial output.",
      "date_discovered": "2026-03-14",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "6bd3e517-9162-4258-a467-d0a687a3e550"
    },
    {
      "id": "FAIL-021",
      "category": "build",
      "title": "Research output обрезан на полуслове — downstream агенты получают неполный контекст",
      "symptoms": "s0-research завершился с 5.5/10 после 3 попыток (2 retries). Документ обрезан на слове \"— оп\", отсутствуют разделы competitive analysis провайдеров и, вероятно, compliance/security секции. Несмотря на это, output был принят с предупреждением и передан дальше. Это значит все последующие агенты (PM, Architect, Cyber) работали бы с заведомо неполной базой знаний.",
      "root_cause": "Research-Agent превысил output limit модели при попытке сгенерировать полный документ за один вызов. Три попытки дали одинаковый результат — системная проблема с размером output, а не случайный сбой. Порог приёмки был снижен до 5.5 вместо требуемых 8+, что создало \"тихое\" распространение некачественных данных по всему pipeline.",
      "solution": "(1) Research-Agent должен генерировать output по секциям (max 3 секции за вызов), затем Orchestrator конкатенирует. (2) Ввести правило: если research score < 7 — pipeline НЕ стартует, возвращается к пользователю с запросом упростить scope или разбить задачу. (3) Детектировать обрыв на полуслове программно (проверка: последний символ не является завершающим знаком препинания или markdown-блоком) и автоматически запрашивать продолжение с \"continue from: [last complete sentence]\".",
      "date_discovered": "2026-03-14",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "6bd3e517-9162-4258-a467-d0a687a3e550"
    },
    {
      "id": "FAIL-022",
      "category": "build",
      "title": "Принятие sub-threshold output с \"warning\" вместо hard stop создаёт downstream cascade",
      "symptoms": "Orchestrator принял s0-research с оценкой 5.5/10 (ниже установленного порога 8+) с формулировкой \"Accepted with warning\". Это решение было принято осознанно, однако привело к тому, что PM-Agent в s2 получил неполный контекст и, возможно, это стало contributing factor к его abort. Паттерн \"принять с предупреждением\" создаёт иллюзию прогресса при реальной деградации качества.",
      "root_cause": "Отсутствие чёткого правила: при каком минимальном score допустим \"accept with warning\" vs hard stop. Orchestrator имеет дискрецию, но без guardrails эта дискреция используется в сторону \"продолжить\" вместо \"остановить\".",
      "solution": "Ввести явную политику: score 6-7 → \"accept with warning\" допустимо только если недостающие секции не являются входными данными для следующего агента. Score < 6 → hard stop всегда, без исключений. Для данного случая: research с отсутствующим competitive analysis и security секциями напрямую влияет на PM (acceptance criteria) и Cyber (threat model) — должен быть hard stop.",
      "date_discovered": "2026-03-14",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "6bd3e517-9162-4258-a467-d0a687a3e550"
    },
    {
      "id": "FAIL-023",
      "category": "build",
      "title": "Frontend Agent Использует [ASSUMED] Вместо Чтения Реального Кода",
      "symptoms": "В выводе s4-frontend явно присутствует маркер `[ASSUMED]` — агент предположил существование кнопки `Export JSON` в Execution History вместо того, чтобы прочитать реальный файл компонента. Решение описано как \"пример кода для внедрения\", а не как готовый diff/патч.",
      "root_cause": "Агент не получил в INPUT конкретный путь к файлу компонента и его текущий код. Task Brief не содержал ссылки на существующий source file — агент был вынужден генерировать решение на основе допущений.",
      "solution": "При делегировании UI-задач Orchestrator обязан включать в INPUT: (1) точный путь к файлу компонента, (2) текущий код кнопки/элемента, (3) используемый CSS-подход (Tailwind / CSS Modules / styled-components). Без этого агент производит \"воздушный код\" который требует ручной адаптации. Формат: `INPUT: file=components/ExecutionHistory/ExportButton.tsx, current_code=<snippet>`.",
      "date_discovered": "2026-03-14",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "be9a7334-27a3-49f5-8eee-a0bbe1b3c3cb"
    },
    {
      "id": "FAIL-024",
      "category": "build",
      "title": "Assumed UI structure без чтения реального кода компонента",
      "symptoms": "Frontend-агент явно указал `[ASSUMED]` — он предположил структуру существующей кнопки Export JSON (иконка, расположение в DOM), не имея доступа к реальному файлу компонента. Решение было описательным («можно встроить»), а не прямой правкой файла.",
      "root_cause": "Оркестратор не передал в INPUT конкретный путь к файлу компонента и его текущий код. Агент работал на основе предположений о структуре UI.",
      "solution": "При делегировании задач на изменение существующего UI всегда включать в INPUT: (1) путь к файлу компонента, (2) текущий код целевого элемента, (3) скриншот или описание текущего состояния. Без этого оценка 10/10 отражает качество описания решения, а не качество реальной интеграции.",
      "date_discovered": "2026-03-14",
      "recurrence": "Discovered during pipeline execution",
      "source_pipeline": "214b5c5a-1ecc-4b87-951d-969086675e6b"
    },
    {
      "id": "FAIL-025",
      "category": "security",
      "title": "GET /api/system/config has no authentication — publicly accessible endpoint",
      "symptoms": "Any unauthenticated HTTP client can call GET /api/system/config and receive system metadata. The route.ts has zero auth checks, no session validation, no middleware guard.",
      "root_cause": "The route handler calls readUIConfig() directly without verifying the caller's session. The comment in the file claims rate limiting is enforced via Next.js middleware, but no middleware file was found protecting /api/system/* routes.",
      "solution": "Add session check at the top of the GET handler: `const session = await getServerSession(); if (!session) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });`. Alternatively, add /api/system/* to the auth middleware matcher. Verify middleware.ts actually covers this path.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-026",
      "category": "bad-code",
      "title": "TimestampFooter fetch has no timeout — hangs indefinitely on slow network",
      "symptoms": "The useEffect fetch to /api/system/config has no AbortController / timeout. On a slow or stalled connection the footer never renders (isLoaded stays false), and the request is never cancelled when the component unmounts, causing a state-update-on-unmounted-component warning.",
      "root_cause": "fetch() is called without an AbortSignal. No cleanup function is returned from useEffect, so the promise resolves after unmount and calls setLastUpdated / setIsLoaded on a dead component.",
      "solution": "Add `const controller = new AbortController(); const timeoutId = setTimeout(() => controller.abort(), 5000);` and pass `{ signal: controller.signal }` to fetch. Return `() => { clearTimeout(timeoutId); controller.abort(); }` from useEffect.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "components/shell/timestamp-footer.tsx"
      ]
    },
    {
      "id": "FAIL-027",
      "category": "bad-code",
      "title": "cached:true flag semantics are inverted and misleading — causes silent data staleness",
      "symptoms": "The API response field `cached: false` means \"fresh data\" and `cached: true` means \"error fallback / stale\". This is the opposite of every HTTP caching convention. Consumers (current or future) will misread the flag and treat error-fallback data as authoritative cached data.",
      "root_cause": "The field was named `cached` but its value encodes an error state, not a cache hit. The JSDoc comment even admits this: \"cached: true → FS read error or JSON parse error (fallback)\".",
      "solution": "Rename the field to `error: boolean` or `fallback: boolean`, or add a separate `error` field. Update the interface, the JSDoc, and any consumers. The TimestampFooter currently ignores this field entirely, so it cannot distinguish stale-fallback from fresh data.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts",
        "components/shell/timestamp-footer.tsx"
      ]
    },
    {
      "id": "FAIL-028",
      "category": "bad-code",
      "title": "ADR-002 violation: getUiConfig() silently swallows ENOENT instead of returning null cleanly",
      "symptoms": "getUiConfig() calls ensureDataDir() before every read, creating the directory on every GET request even when the file doesn't exist. The catch block returns null correctly, but the directory-creation side-effect on every read is unexpected and masks the ENOENT condition instead of propagating it as a clean null per ADR-002.",
      "root_cause": "ensureDataDir() is called inside getUiConfig() unconditionally. ADR-002 says ENOENT should return null, not trigger auto-creation. The directory mkdir is a write side-effect inside a read function, violating command-query separation.",
      "solution": "Remove ensureDataDir() call from getUiConfig(). Only call it inside setUiConfig(). In getUiConfig(), catch ENOENT specifically (err.code === 'ENOENT') and return {lastUpdated: null}; re-throw other errors so the route's catch block can log them properly.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/ui-config-storage.ts",
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-029",
      "category": "security",
      "title": "POST /api/system/config has no authentication — any unauthenticated caller can overwrite lastUpdated",
      "symptoms": "POST /api/system/config accepts any body with a valid ISO string and writes it to disk. No session check, no API key, no internal-only guard. Any external actor can set lastUpdated to an arbitrary date (e.g. year 9999) causing misleading UI across all shell pages.",
      "root_cause": "Route handler has no auth middleware or token verification. The comment says \"fire-and-forget from pipeline-executor\" but the route is publicly reachable.",
      "solution": "Add an internal secret check: compare Authorization header against process.env.INTERNAL_API_SECRET. Return 401 if missing/wrong. Alternatively, move the write to a server-action callable only from pipeline-executor server-side code, removing the HTTP surface entirely.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-030",
      "category": "bad-code",
      "title": "setUiConfig atomic write leaves orphaned .tmp file on rename failure",
      "symptoms": "If fs.rename() throws (e.g. cross-device link, permissions), the .tmp file is left on disk permanently. Subsequent calls to setUiConfig will overwrite it, but a crash between writeFile and rename leaves DATA_CONFIG_PATH.tmp polluting the data/config/ directory indefinitely.",
      "root_cause": "No finally block cleans up the tmp file on rename failure. The error propagates to the caller but the orphaned file remains.",
      "solution": "Wrap the rename in try/catch; in the catch, attempt fs.unlink(tmpPath) before re-throwing. Use a unique tmp name (e.g. append process.pid + Date.now()) to avoid collisions under concurrent writes.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/ui-config-storage.ts"
      ]
    },
    {
      "id": "FAIL-031",
      "category": "security",
      "title": "POST /api/system/config has no authentication — unauthenticated write to filesystem",
      "symptoms": "Any anonymous HTTP client can POST { lastUpdated: \"2099-01-01T00:00:00Z\" } and overwrite data/config/ui.json, poisoning the timestamp shown to all users in the shell footer",
      "root_cause": "route.ts exports POST handler with no auth guard, no middleware check, no API key validation. Next.js App Router does not apply auth middleware automatically to /api routes unless explicitly configured in middleware.ts matcher.",
      "solution": "Add server-side auth check at top of POST handler: verify session via getServerSession() or validate an internal CRON_SECRET header. Add /api/system/config to middleware.ts protected routes matcher. Rate-limit POST to 10 req/min per IP as documented but not implemented.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-032",
      "category": "security",
      "title": "Missing rate limiting implementation — documented but not enforced on /api/system/config",
      "symptoms": "Route comments claim \"Rate limit: 60 req/min per IP\" (GET) and \"Rate limit: 10 req/min per IP\" (POST) but no rate-limiting middleware, header checks, or Upstash/Redis integration exists in the handler code",
      "root_cause": "Rate limit is documented as a comment only. No actual implementation (e.g., upstash/ratelimit, next-rate-limit, or custom Redis counter) is present in the route file or called from a middleware wrapper.",
      "solution": "Implement rate limiting using @upstash/ratelimit or a sliding-window Redis counter. Apply as a wrapper or in middleware.ts. Enforce 429 response with Retry-After header when limit exceeded.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-033",
      "category": "security",
      "title": "POST /api/system/config — нет аутентификации, любой может перезаписать timestamp",
      "symptoms": "Эндпоинт POST /api/system/config принимает тело без проверки токена/сессии. Любой внешний клиент может отправить произвольный ISO-timestamp и перезаписать данные конфигурации.",
      "root_cause": "В route.ts отсутствует middleware аутентификации или проверка заголовка Authorization/API-key перед обработкой POST-запроса.",
      "solution": "Добавить проверку внутреннего секрета (INTERNAL_API_SECRET из env) в заголовке Authorization: Bearer <secret> перед обработкой тела. Вернуть 401 при несовпадении. Альтернатива — ограничить маршрут только серверным вызовом через server action.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-034",
      "category": "security",
      "title": "GET /api/system/config — нет rate limiting, возможен DoS через FS-чтение",
      "symptoms": "Комментарий в коде обещает «Rate limit: 60 req/min per IP», но реальной реализации rate limiting в route.ts нет — ни middleware, ни заголовков, ни счётчика. Эндпоинт читает файл с диска на каждый запрос.",
      "root_cause": "Rate limit задокументирован в JSDoc-комментарии, но не реализован в коде. Нет ни next-rate-limit, ни edge middleware, ни Redis-счётчика.",
      "solution": "Реализовать rate limiting через Next.js middleware (middleware.ts) с использованием @upstash/ratelimit или аналога. Добавить заголовки Retry-After и X-RateLimit-Remaining в ответ.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-035",
      "category": "bad-code",
      "title": "data/config/ui.json отсутствует в репозитории — первый запуск вернёт null без инициализации",
      "symptoms": "Директория data/config не существует (ENOENT при list_files). При первом запуске getUiConfig() выбросит ошибку чтения файла, GET вернёт { lastUpdated: null, error: \"config_unavailable\" }, футер не отобразится никогда до первого pipeline-запуска.",
      "root_cause": "Файл data/config/ui.json не создан как часть задачи (задача требовала его создать). Нет seed-скрипта или инициализации при старте приложения.",
      "solution": "1) Создать data/config/ui.json с начальным значением { \"lastUpdated\": \"&lt;build-time ISO&gt;\" }. 2) Добавить файл в .gitignore исключения (не игнорировать). 3) В getUiConfig() добавить fallback: если файл не существует — вернуть { lastUpdated: null } без throw.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "data/config/ui.json",
        "lib/ui-config-storage.ts"
      ]
    },
    {
      "id": "FAIL-036",
      "category": "bad-code",
      "title": "TimestampFooter returns null during loading — permanent blank state if API never resolves",
      "symptoms": "Footer never renders if SWR fetch hangs or API returns { lastUpdated: null }. Component returns null for both isLoading AND formattedDate === null, so a successful API response with null lastUpdated also produces no footer — silently swallowing the \"no data\" state with no user feedback.",
      "root_cause": "Line 45 of timestamp-footer.tsx: `if (isLoading || formattedDate === null) return null` — the null guard conflates \"still loading\" with \"loaded but no timestamp\". When config file is missing and API returns { lastUpdated: null }, formattedDate stays null and the footer is permanently invisible.",
      "solution": "Separate loading and empty states. When !isLoading and data is received but lastUpdated is null, render a fallback string (e.g. \"Дата обновления недоступна\") or at minimum a non-null placeholder. Only suppress render during isLoading.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "components/shell/timestamp-footer.tsx"
      ]
    },
    {
      "id": "FAIL-037",
      "category": "bad-code",
      "title": "GET /api/system/config accesses config.lastUpdated without null-guard when getUiConfig returns null per ADR-002",
      "symptoms": "Route.ts line 33: `{ lastUpdated: config.lastUpdated }` — if getUiConfig() returns null (per ADR-002 ENOENT behavior), this throws a TypeError: Cannot read properties of null, which is caught by the outer catch and returns config_unavailable — masking the expected null response as an error.",
      "root_cause": "ADR-002 specifies getUiConfig() should return null on ENOENT, but the GET handler treats the return value as always being an object with a lastUpdated property. No null-check before property access on line 33.",
      "solution": "Add null guard: `const lastUpdated = config ? config.lastUpdated : null; return NextResponse.json({ lastUpdated })`. This correctly returns { lastUpdated: null } (200 OK) when file is missing, matching ADR-002 contract.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-038",
      "category": "security",
      "title": "POST /api/system/config — нет аутентификации, любой может перезаписать lastUpdated",
      "symptoms": "Эндпоинт POST /api/system/config принимает запросы без проверки токена/сессии. Любой анонимный пользователь может отправить произвольную дату и изменить отображаемый timestamp во всём shell-layout.",
      "root_cause": "В route.ts отсутствует middleware-проверка авторизации (getServerSession / verifyToken). Нет проверки роли — достаточно знать URL.",
      "solution": "Добавить проверку сессии в начало POST-обработчика: const session = await getServerSession(); if (!session) return NextResponse.json({ok:false,error:'unauthorized'},{status:401}). Ограничить доступ ролью ADMIN или SYSTEM.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    }
  ]
}