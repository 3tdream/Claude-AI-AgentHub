{
  "_description": "Known issues, root causes, and solutions — maintained by QA-Agent and all agents",
  "_updated": "2026-03-22",
  "patterns": [
    {
      "id": "FAIL-001",
      "category": "build",
      "title": "Next.js version mismatch in monorepo",
      "symptoms": "Build fails with 'Jest worker encountered child process exceptions' or Turbopack internal error",
      "root_cause": "Root monorepo has next@16.1.1, mission-control needs 15.5.4. npx resolves to root version.",
      "solution": "Use node_modules/.bin/next build instead of npx next build",
      "date_discovered": "2026-03-10",
      "recurrence": "Every time someone runs npx next"
    },
    {
      "id": "FAIL-002",
      "category": "hydration",
      "title": "SSR hydration mismatch with dynamic values",
      "symptoms": "React hydration error: server rendered text didn't match client",
      "root_cause": "Using Date.now(), Math.random(), or locale-dependent formatting in SSR components",
      "solution": "Move dynamic values to useEffect or pass stable values from server",
      "date_discovered": "2026-03",
      "recurrence": "When adding date displays or random IDs in page components"
    },
    {
      "id": "FAIL-003",
      "category": "api",
      "title": "Agent Hub connection timeout",
      "symptoms": "Chat returns OpenAI fallback, API routes return cached: true",
      "root_cause": "Agent Hub server on localhost:3000 not running or network timeout (3s limit)",
      "solution": "Start Agent Hub server. Check AGENT_HUB_LIVE env var. Cached data serves as fallback.",
      "date_discovered": "2026-02",
      "recurrence": "After system restart or when Agent Hub is updating"
    },
    {
      "id": "FAIL-004",
      "category": "api",
      "title": "Agent Hub execute returns empty on long analytical prompts",
      "symptoms": "MCP execute call returns empty content for prompts requesting structured analysis (8+ lesson extraction)",
      "root_cause": "Likely token limit or timeout on stateless /execute endpoint. Agent starts tool calls but response exceeds output token limit (4000-6000 tokens).",
      "solution": "Break large prompts into smaller focused queries. Or use session-based chat for long interactions. Orchestrator hello works (short response), analysis fails (long response).",
      "date_discovered": "2026-03-12",
      "recurrence": "When using MCP execute for complex analytical tasks",
      "source_project": "mission-control"
    },
    {
      "id": "FAIL-005",
      "category": "devops",
      "title": "pnpm hard-link corruption on Windows 11",
      "symptoms": "node_modules files show as directories with year 1601 timestamps. npm install fails.",
      "root_cause": "pnpm uses hard-links from global store. Windows NTFS sometimes corrupts these links during forced shutdown or antivirus scan.",
      "solution": "PowerShell Remove-Item -Recurse -Force on node_modules, then npm install --no-workspaces instead of pnpm.",
      "date_discovered": "2026-03-12",
      "recurrence": "On Windows systems using pnpm with hard-links"
    },
    {
      "id": "FAIL-025",
      "category": "security",
      "title": "GET /api/system/config has no authentication — publicly accessible endpoint",
      "symptoms": "Any unauthenticated HTTP client can call GET /api/system/config and receive system metadata. The route.ts has zero auth checks, no session validation, no middleware guard.",
      "root_cause": "The route handler calls readUIConfig() directly without verifying the caller's session. The comment in the file claims rate limiting is enforced via Next.js middleware, but no middleware file was found protecting /api/system/* routes.",
      "solution": "Add session check at the top of the GET handler: `const session = await getServerSession(); if (!session) return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });`. Alternatively, add /api/system/* to the auth middleware matcher. Verify middleware.ts actually covers this path.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-026",
      "category": "bad-code",
      "title": "TimestampFooter fetch has no timeout — hangs indefinitely on slow network",
      "symptoms": "The useEffect fetch to /api/system/config has no AbortController / timeout. On a slow or stalled connection the footer never renders (isLoaded stays false), and the request is never cancelled when the component unmounts, causing a state-update-on-unmounted-component warning.",
      "root_cause": "fetch() is called without an AbortSignal. No cleanup function is returned from useEffect, so the promise resolves after unmount and calls setLastUpdated / setIsLoaded on a dead component.",
      "solution": "Add `const controller = new AbortController(); const timeoutId = setTimeout(() => controller.abort(), 5000);` and pass `{ signal: controller.signal }` to fetch. Return `() => { clearTimeout(timeoutId); controller.abort(); }` from useEffect.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "components/shell/timestamp-footer.tsx"
      ]
    },
    {
      "id": "FAIL-027",
      "category": "bad-code",
      "title": "cached:true flag semantics are inverted and misleading — causes silent data staleness",
      "symptoms": "The API response field `cached: false` means \"fresh data\" and `cached: true` means \"error fallback / stale\". This is the opposite of every HTTP caching convention. Consumers (current or future) will misread the flag and treat error-fallback data as authoritative cached data.",
      "root_cause": "The field was named `cached` but its value encodes an error state, not a cache hit. The JSDoc comment even admits this: \"cached: true → FS read error or JSON parse error (fallback)\".",
      "solution": "Rename the field to `error: boolean` or `fallback: boolean`, or add a separate `error` field. Update the interface, the JSDoc, and any consumers. The TimestampFooter currently ignores this field entirely, so it cannot distinguish stale-fallback from fresh data.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts",
        "components/shell/timestamp-footer.tsx"
      ]
    },
    {
      "id": "FAIL-028",
      "category": "bad-code",
      "title": "ADR-002 violation: getUiConfig() silently swallows ENOENT instead of returning null cleanly",
      "symptoms": "getUiConfig() calls ensureDataDir() before every read, creating the directory on every GET request even when the file doesn't exist. The catch block returns null correctly, but the directory-creation side-effect on every read is unexpected and masks the ENOENT condition instead of propagating it as a clean null per ADR-002.",
      "root_cause": "ensureDataDir() is called inside getUiConfig() unconditionally. ADR-002 says ENOENT should return null, not trigger auto-creation. The directory mkdir is a write side-effect inside a read function, violating command-query separation.",
      "solution": "Remove ensureDataDir() call from getUiConfig(). Only call it inside setUiConfig(). In getUiConfig(), catch ENOENT specifically (err.code === 'ENOENT') and return {lastUpdated: null}; re-throw other errors so the route's catch block can log them properly.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/ui-config-storage.ts",
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-029",
      "category": "security",
      "title": "POST /api/system/config has no authentication — any unauthenticated caller can overwrite lastUpdated",
      "symptoms": "POST /api/system/config accepts any body with a valid ISO string and writes it to disk. No session check, no API key, no internal-only guard. Any external actor can set lastUpdated to an arbitrary date (e.g. year 9999) causing misleading UI across all shell pages.",
      "root_cause": "Route handler has no auth middleware or token verification. The comment says \"fire-and-forget from pipeline-executor\" but the route is publicly reachable.",
      "solution": "Add an internal secret check: compare Authorization header against process.env.INTERNAL_API_SECRET. Return 401 if missing/wrong. Alternatively, move the write to a server-action callable only from pipeline-executor server-side code, removing the HTTP surface entirely.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-030",
      "category": "bad-code",
      "title": "setUiConfig atomic write leaves orphaned .tmp file on rename failure",
      "symptoms": "If fs.rename() throws (e.g. cross-device link, permissions), the .tmp file is left on disk permanently. Subsequent calls to setUiConfig will overwrite it, but a crash between writeFile and rename leaves DATA_CONFIG_PATH.tmp polluting the data/config/ directory indefinitely.",
      "root_cause": "No finally block cleans up the tmp file on rename failure. The error propagates to the caller but the orphaned file remains.",
      "solution": "Wrap the rename in try/catch; in the catch, attempt fs.unlink(tmpPath) before re-throwing. Use a unique tmp name (e.g. append process.pid + Date.now()) to avoid collisions under concurrent writes.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/ui-config-storage.ts"
      ]
    },
    {
      "id": "FAIL-032",
      "category": "security",
      "title": "Missing rate limiting implementation — documented but not enforced on /api/system/config",
      "symptoms": "Route comments claim \"Rate limit: 60 req/min per IP\" (GET) and \"Rate limit: 10 req/min per IP\" (POST) but no rate-limiting middleware, header checks, or Upstash/Redis integration exists in the handler code",
      "root_cause": "Rate limit is documented as a comment only. No actual implementation (e.g., upstash/ratelimit, next-rate-limit, or custom Redis counter) is present in the route file or called from a middleware wrapper.",
      "solution": "Implement rate limiting using @upstash/ratelimit or a sliding-window Redis counter. Apply as a wrapper or in middleware.ts. Enforce 429 response with Retry-After header when limit exceeded.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-035",
      "category": "bad-code",
      "title": "data/config/ui.json отсутствует в репозитории — первый запуск вернёт null без инициализации",
      "symptoms": "Директория data/config не существует (ENOENT при list_files). При первом запуске getUiConfig() выбросит ошибку чтения файла, GET вернёт { lastUpdated: null, error: \"config_unavailable\" }, футер не отобразится никогда до первого pipeline-запуска.",
      "root_cause": "Файл data/config/ui.json не создан как часть задачи (задача требовала его создать). Нет seed-скрипта или инициализации при старте приложения.",
      "solution": "1) Создать data/config/ui.json с начальным значением { \"lastUpdated\": \"&lt;build-time ISO&gt;\" }. 2) Добавить файл в .gitignore исключения (не игнорировать). 3) В getUiConfig() добавить fallback: если файл не существует — вернуть { lastUpdated: null } без throw.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "data/config/ui.json",
        "lib/ui-config-storage.ts"
      ]
    },
    {
      "id": "FAIL-036",
      "category": "bad-code",
      "title": "TimestampFooter returns null during loading — permanent blank state if API never resolves",
      "symptoms": "Footer never renders if SWR fetch hangs or API returns { lastUpdated: null }. Component returns null for both isLoading AND formattedDate === null, so a successful API response with null lastUpdated also produces no footer — silently swallowing the \"no data\" state with no user feedback.",
      "root_cause": "Line 45 of timestamp-footer.tsx: `if (isLoading || formattedDate === null) return null` — the null guard conflates \"still loading\" with \"loaded but no timestamp\". When config file is missing and API returns { lastUpdated: null }, formattedDate stays null and the footer is permanently invisible.",
      "solution": "Separate loading and empty states. When !isLoading and data is received but lastUpdated is null, render a fallback string (e.g. \"Дата обновления недоступна\") or at minimum a non-null placeholder. Only suppress render during isLoading.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "components/shell/timestamp-footer.tsx"
      ]
    },
    {
      "id": "FAIL-037",
      "category": "bad-code",
      "title": "GET /api/system/config accesses config.lastUpdated without null-guard when getUiConfig returns null per ADR-002",
      "symptoms": "Route.ts line 33: `{ lastUpdated: config.lastUpdated }` — if getUiConfig() returns null (per ADR-002 ENOENT behavior), this throws a TypeError: Cannot read properties of null, which is caught by the outer catch and returns config_unavailable — masking the expected null response as an error.",
      "root_cause": "ADR-002 specifies getUiConfig() should return null on ENOENT, but the GET handler treats the return value as always being an object with a lastUpdated property. No null-check before property access on line 33.",
      "solution": "Add null guard: `const lastUpdated = config ? config.lastUpdated : null; return NextResponse.json({ lastUpdated })`. This correctly returns { lastUpdated: null } (200 OK) when file is missing, matching ADR-002 contract.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-039",
      "category": "security",
      "title": "Escalation trigger bypass via metadata injection in stepResult",
      "symptoms": "Malicious or malformed agent output can inject arbitrary metadata fields (severity, hasCriticalBug, qualityScore) into stepResult.metadata, triggering or suppressing escalation triggers T1-T5 without legitimate security/QA findings.",
      "root_cause": "checkEscalationTriggers() trusts stepResult.metadata values without validation or schema enforcement. Any agent response that populates metadata fields can control escalation flow. The parseEscalateTag regex is also injectable via crafted agent output.",
      "solution": "1) Validate stepResult.metadata against a strict schema (zod) before trigger evaluation. 2) Only allow specific agentIds to set specific metadata fields (cyber-agent → severity/probability, qa-agent → hasCriticalBug/qualityScore). 3) Add HMAC or signing to agent outputs to prevent tampering between step execution and trigger check. 4) Sanitize parseEscalateTag input — limit output length before regex parsing.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/pipeline-executor.ts",
        "types/workflow.ts"
      ]
    },
    {
      "id": "FAIL-042",
      "category": "security",
      "title": "Zustand persist stores PII (escalation data) unencrypted in localStorage",
      "symptoms": "EscalationReason[], confirmedBy (MEDIUM PII), security finding severities stored in plaintext localStorage via Zustand persist middleware. Accessible via browser DevTools.",
      "root_cause": "orchestration-store.ts uses Zustand persist without partialize filter or encrypted storage adapter. New ADR-012 fields (escalationReasons, escalatedFrom) automatically persisted with full detail strings.",
      "solution": "1) Use partialize in persist config to exclude escalationReasons.detail and confirmedBy from localStorage. 2) Or implement encrypted storage adapter (AES-256 with session-derived key). 3) Strip sensitive fields in addToHistory before persisting to executionHistory array. This is a 152-FZ Art.19 requirement.",
      "date_discovered": "2026-03-16",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/stores/orchestration-store.ts",
        "types/workflow.ts"
      ]
    },
    {
      "id": "FAIL-045",
      "category": "bad-code",
      "title": "Stateful /g regex in ESCALATION_CONFIG causes intermittent security trigger misses",
      "symptoms": "detectEscalation() fails to detect Critical/High security findings approximately 50% of the time; escalation triggers fire inconsistently across consecutive pipeline steps.",
      "root_cause": "ESCALATION_CONFIG stores regex patterns with the /g flag. RegExp.test() on a global regex advances lastIndex; on the next call with the same regex instance, matching starts from the previous lastIndex position, often returning false.",
      "solution": "Either store patterns as plain strings and create new RegExp() inside detectEscalation() on each invocation, or remove the /g flag from securityCriticalPattern and securityHighPattern since .test() only needs a boolean match.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/config.ts",
        "lib/smart-router.ts"
      ]
    },
    {
      "id": "FAIL-046",
      "category": "security",
      "title": "ADR-008 detectEscalation: stateful /g regex, output injection, XSS in badge, Jira comment injection",
      "symptoms": "1) Stateful /g regex in ESCALATION_CONFIG causes .test() to miss matches ~50% of calls. 2) Free-text stepResult.output parsed by regex allows any agent to inject trigger keywords forcing false escalation. 3) EscalationBadge renders trigger.reason (agent-controlled string) without sanitization — XSS risk. 4) Jira comment includes unsanitized reason field — ADF injection. 5) No auth on Zustand setEscalation action.",
      "root_cause": "Security detection relies on regex over unstructured text instead of typed severity fields. UI renders agent-controlled strings directly. Jira comment template interpolates untrusted data into ADF. Global regex flag preserves lastIndex across calls.",
      "solution": "1) Remove /g flag or instantiate new RegExp per call. 2) Use structured stepResult.severity field, not regex on output. 3) Sanitize trigger.reason before DOM rendering (escape HTML entities). 4) Sanitize reason before Jira ADF interpolation. 5) Validate executionId ownership in setEscalation.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/config.ts",
        "lib/smart-router.ts",
        "lib/pipeline-executor.ts",
        "components/escalation-badge.tsx",
        "lib/jira-sync-service.ts",
        "lib/stores/orchestration-store.ts"
      ]
    },
    {
      "id": "FAIL-048",
      "category": "security",
      "title": "POST /api/system/config — no authentication, open write endpoint",
      "symptoms": "POST /api/system/config accepts writes from any anonymous client. No token, no secret, no session check. Returns {\"ok\":true} for unauthenticated requests.",
      "root_cause": "Route handler has zero authentication middleware. The endpoint is documented as \"fire-and-forget from pipeline-executor\" but no internal secret or auth guard was implemented. Rate limit exists only as a JSDoc comment with no actual code.",
      "solution": "Add x-internal-secret header check: const secret = req.headers.get(\"x-internal-secret\"); if (secret !== process.env.INTERNAL_API_SECRET) return 401. Generate INTERNAL_API_SECRET at deploy time (32 bytes). pipeline-executor.ts must pass this header. Also validate lastUpdated strictly with ISO 8601 regex before passing to setUiConfig().",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/system/config/route.ts"
      ]
    },
    {
      "id": "FAIL-049",
      "category": "security",
      "title": "CSV Export без санитизации — XSS через CSV Injection в pipeline-analytics.json",
      "symptoms": "Экспортируемый CSV содержит поля agentName/stepId из pipeline-analytics.json без экранирования. Значения вида =CMD|' /C calc'!A0 или @SUM(1+1) выполняются в Excel/LibreOffice при открытии файла.",
      "root_cause": "Архитектура экспорта предполагает прямую сериализацию объектов PipelineExecution и agentStats в CSV без префиксного экранирования специальных символов (=, +, -, @, TAB, CR).",
      "solution": "Перед записью в CSV каждое строковое поле проверять: если начинается с = + - @ TAB CR — добавлять префикс апострофа или оборачивать в кавычки с экранированием. Использовать библиотеку papaparse или ручную sanitize-функцию.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "data/pipeline-analytics.json",
        "app/(shell)/analytics/page.tsx"
      ]
    },
    {
      "id": "FAIL-050",
      "category": "security",
      "title": "No auth middleware on internal API routes — all agent-hub proxy routes are publicly accessible",
      "symptoms": "All routes under /api/agent-hub/ (sessions, agents, last-activity) have zero authentication checks. Any external caller who discovers the URL can read agent session data, activity timestamps, and agent metadata.",
      "root_cause": "The codebase has no auth middleware layer (no NextAuth, no session cookies, no internal API key validation). The existing sessions/route.ts pattern that the new endpoint copies from also lacks auth. This is a systemic gap, not a single-endpoint issue.",
      "solution": "Create a shared middleware (e.g., lib/middleware/require-auth.ts) that validates session cookies or an internal token header. Apply it to all /api/agent-hub/* routes. Until auth exists, add Next.js middleware.ts matcher to restrict /api/agent-hub/* to same-origin requests via Origin/Referer header check as a stopgap.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/agent-hub/sessions/route.ts",
        "app/api/agent-hub/agents/last-activity/route.ts",
        "lib/agent-hub-client.ts"
      ]
    },
    {
      "id": "FAIL-051",
      "category": "security",
      "title": "All /api/agent-hub/* routes lack authentication — any HTTP client can read agent session/activity data",
      "symptoms": "No auth check in sessions/route.ts or proposed last-activity/route.ts. No middleware.ts protecting /api/ routes. agentHubFetch uses server-side API key to Agent Hub but the Next.js API route itself is publicly accessible.",
      "root_cause": "Project has no auth system implemented yet. API routes were created as internal proxies but are exposed on the public Next.js server without any request validation. The agentIds query param is concatenated into URLs without UUID format validation, enabling potential SSRF via agentHubFetch URL construction.",
      "solution": "1) Create middleware.ts with matcher for /api/agent-hub/:path* that checks for a session cookie (when auth exists) or at minimum validates X-Requested-With: XMLHttpRequest header + Origin header matching NEXT_PUBLIC_APP_URL. 2) In last-activity/route.ts, validate agentIds with regex /^[0-9a-f-]{36}(,[0-9a-f-]{36})*$/ before passing to any fetch. 3) Add structured logging (pino or project logger) with fields: { endpoint, agentIds, ip, userAgent, timestamp } to stdout for 152-FZ Art.19 audit trail. 4) Classify lastActivity as MEDIUM PII in 152fz-checklist.md and ensure no Supabase caching.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/agent-hub/sessions/route.ts",
        "app/api/agent-hub/agents/last-activity/route.ts",
        "lib/agent-hub-client.ts",
        "middleware.ts"
      ]
    },
    {
      "id": "FAIL-052",
      "category": "security",
      "title": "All /api/agent-hub/* routes lack authentication — public data exposure",
      "symptoms": "Any unauthenticated HTTP client can call GET /api/agent-hub/sessions, /agents/last-activity etc. and receive agent session data. No middleware, no cookie check, no token validation exists on these routes.",
      "root_cause": "sessions/route.ts (line 5) and all proposed agent-hub API routes have zero auth checks. agentHubFetch authenticates MC→AgentHub via PAT, but nothing authenticates browser→MC.",
      "solution": "Create middleware.ts with matcher /api/agent-hub/:path* that validates session cookie or, as stopgap, checks Origin + X-Requested-With headers. Must be replaced with real session auth before any public deployment. BLOCKS DEPLOY.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/agent-hub/sessions/route.ts",
        "app/api/agent-hub/agents/last-activity/route.ts"
      ]
    },
    {
      "id": "FAIL-053",
      "category": "security",
      "title": "Unsanitized JSON key from user input in file-based storage (agent-last-login)",
      "symptoms": "POST /api/agent-last-login accepts arbitrary agentId string and uses it as a key in a JSON file without validation, enabling prototype pollution (__proto__) or storage abuse.",
      "root_cause": "No input validation or allowlist check on agentId before merging into JSON object and writing to disk.",
      "solution": "Validate agentId with strict regex /^[a-z0-9-]{1,64}$/ and reject requests that don't match. Also use Object.create(null) when parsing JSON to prevent prototype pollution.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/agent-last-login/route.ts",
        "app/api/chat/route.ts"
      ]
    },
    {
      "id": "FAIL-054",
      "category": "security",
      "title": "Path traversal and race condition in agent-last-login file-based storage",
      "symptoms": "POST /api/agent-last-login accepts arbitrary agentId string used as JSON key with no validation. Concurrent fire-and-forget writes from chat route cause read-modify-write race condition losing timestamps. Self-fetch to localhost:3000 creates SSRF vector.",
      "root_cause": "1) No agentId validation — attacker can inject __proto__, constructor, or oversized keys into JSON via crafted agentId, causing prototype pollution or DoS. 2) Concurrent fs read-modify-write without file locking loses data. 3) Fire-and-forget uses HTTP self-fetch instead of direct function call, exposing internal endpoint.",
      "solution": "1) In POST handler of app/api/agent-last-login/route.ts: validate agentId with regex /^[a-z0-9-]{1,64}$/ AND whitelist against cachedAgents.map(a=>a.id). 2) Use proper-lockfile or a mutex around read-modify-write cycle. 3) In app/api/chat/route.ts: replace self-fetch with direct function import (e.g., import {updateLastLogin} from '@/lib/agent-last-login' and call updateLastLogin(agentId).catch(...)). Reject __proto__ and constructor keys explicitly.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/agent-last-login/route.ts",
        "app/api/chat/route.ts"
      ]
    },
    {
      "id": "FAIL-055",
      "category": "security",
      "title": "File-based storage TOCTOU + input validation gaps in agent-last-login",
      "symptoms": "Concurrent POST /api/agent-last-login requests lose timestamps; unsanitized agentId allows prototype pollution via __proto__ key; self-fetch to localhost:3000 breaks in production and exposes internal endpoints.",
      "root_cause": "PAT-002 file-based storage pattern lacks concurrency control. No input validation on agentId before writing to JSON. Fire-and-forget uses HTTP self-fetch instead of direct module import.",
      "solution": "1) Add mutex/lock around read-modify-write in POST handler. 2) Validate agentId with /^[a-z0-9-]{1,64}$/ regex. 3) Extract file I/O into lib/agent-last-login.ts shared module, import directly in chat/route.ts instead of self-fetch.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/agent-last-login/route.ts",
        "app/api/chat/route.ts",
        "lib/agent-last-login.ts"
      ]
    },
    {
      "id": "FAIL-056",
      "category": "security",
      "title": "Regex-based escalation detection on LLM output is bypassable and injectable",
      "symptoms": "EscalationDetector T1-T5 triggers use regex on raw LLM agent output strings. Malicious or malformed agent output can either suppress escalation (bypass) or force false escalation (injection).",
      "root_cause": "T1/T2 use regex like /\\\"severity\\\"\\s*:\\s*\\\"critical\\\"/i on unstructured StepResult.output. LLM output is not validated against a schema before regex matching. An adversarial prompt or hallucinated output could embed \"severity\":\"low\" while containing critical findings, or inject [ESCALATE] tags (T5) to force unnecessary escalation and waste tokens.",
      "solution": "Parse agent output into a typed object via zod schema validation BEFORE running trigger detection. Never regex raw LLM strings for security-critical decisions. Add output schema enforcement in StepResult so triggers operate on structured, validated data.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/escalation-detector.ts",
        "types/workflow.ts"
      ]
    },
    {
      "id": "FAIL-057",
      "category": "security",
      "title": "Escalation detector uses regex on untrusted agent output — injection + XSS + privilege escalation risks",
      "symptoms": "ADR-008 escalation-detector.ts parses raw agent output with regex to detect triggers (T1-T5). Agent output is untrusted LLM text rendered in UI badges and Jira comments. No sanitization, no auth on escalation confirmation, confirmedBy field is MEDIUM PII stored in localStorage via zustand persist.",
      "root_cause": "1) Regex-based detection on LLM output is trivially spoofable — any agent can emit \"severity\":\"critical\" to force T1 escalation. 2) EscalationRecord.triggeredBy.reason is rendered in UI badge and Jira comment without sanitization — XSS vector. 3) No authorization check on who confirms/rejects escalation — any browser session can call approveCheckpoint. 4) confirmedBy (MEDIUM PII per 152-FZ) persisted in localStorage via zustand persist — unencrypted. 5) buildEscalationComment uses string comparison triggerId <= \"T2\" which is lexicographic, not numeric — fragile but not exploitable.",
      "solution": "1) Validate agent output against structured JSON schema, not regex on raw text. 2) Sanitize reason field with DOMPurify before rendering in React and escape for Jira markdown. 3) Add user identity verification before escalation confirm/reject actions. 4) Encrypt or exclude confirmedBy from localStorage persistence. 5) Use explicit set comparison for trigger priority instead of string comparison.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/escalation-detector.ts",
        "components/pipeline/escalation-badge.tsx",
        "lib/jira-sync-service.ts",
        "lib/stores/orchestration-store.ts",
        "types/workflow.ts"
      ]
    },
    {
      "id": "FAIL-058",
      "category": "security",
      "title": "EscalationRecord.confirmedBy PII stored unencrypted in localStorage via zustand persist",
      "symptoms": "MEDIUM PII field (confirmedBy: user identifier) persisted to browser localStorage in plaintext via zustand persist middleware in orchestration-store.ts. executionHistory[] contains full EscalationRecord including confirmedBy. Any XSS or browser extension can read all user identifiers from localStorage.",
      "root_cause": "orchestration-store.ts uses zustand persist (line 4, 25) which serializes entire state including executionHistory[] to localStorage. EscalationRecord.confirmedBy (workflow.ts:105) is classified MEDIUM PII per 152-FZ but no encryption or redaction is applied before persistence.",
      "solution": "1. Add a custom serialize function to zustand persist config that redacts or encrypts confirmedBy before writing to localStorage. 2. Alternatively, store confirmedBy only server-side (Timeweb VPS) and keep only a non-PII reference in client state. 3. For 152-FZ Art.19 compliance, MEDIUM PII must be AES-256 encrypted at rest — localStorage is not encrypted at rest.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/stores/orchestration-store.ts",
        "types/workflow.ts"
      ]
    },
    {
      "id": "FAIL-059",
      "category": "security",
      "title": "Escalation detector parses unstructured LLM output — prompt injection can force mode escalation",
      "symptoms": "detectEscalation() uses string matching on agent output to detect severity=critical, qualityScore, filesAffected. Crafted task input containing these keywords triggers false escalation to Full mode.",
      "root_cause": "Trigger detection relies on parsing free-text output strings instead of structured, typed metadata fields from stepResults. No validation that the output actually comes from the expected agent.",
      "solution": "1. Use only structured stepResults metadata (typed severity enum, numeric qualityScore) for trigger detection — never parse raw output strings. 2. Validate that trigger source agent matches expected agent ID (T1/T2→cyber-agent, T3/T4→qa-agent, T5→architect-agent). 3. Sanitize reason field before Jira sync to prevent PII leakage to external SaaS.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/escalation-detector.ts",
        "lib/pipeline-executor.ts",
        "lib/jira-sync-service.ts"
      ]
    },
    {
      "id": "FAIL-060",
      "category": "security",
      "title": "ADR-008 Jira sync route missing auth + action whitelist allows SSRF to Jira API",
      "symptoms": "POST /api/jira/sync has no authentication check. Any client can call it with arbitrary action/body, including the new \"mode-escalation\" action. The route also casts body.execution directly to PipelineExecution without validation.",
      "root_cause": "app/api/jira/sync/route.ts has no auth middleware, no origin check, no CSRF token. The new \"mode-escalation\" action will inherit this gap, allowing unauthenticated comment injection into Jira Epics.",
      "solution": "Add same-origin validation (check Origin/Referer header or use internal API token from env). Validate action against explicit whitelist. Validate body schema with zod before passing to jiraSync functions. For \"mode-escalation\": validate triggerId is T1-T5 enum, fromMode/toMode are valid ExecutionMode values.",
      "date_discovered": "2026-03-17",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/jira/sync/route.ts",
        "lib/jira-sync-service.ts",
        "lib/pipeline-executor.ts"
      ]
    },
    {
      "id": "FAIL-061",
      "category": "security",
      "title": "Prototype pollution via unsanitized agentId as object key",
      "symptoms": "Crafted agentId values like __proto__, constructor, or toString used as keys in JSON map can pollute Object prototype when assigned via bracket notation map[agentId] = value",
      "root_cause": "recordAgentActivity(agentId) accepts arbitrary string from request body and uses it directly as object key without validation",
      "solution": "Validate agentId with regex /^[a-zA-Z0-9_-]+$/ and check against known agent list before writing. Apply this pattern to all file-based storage modules that use user-supplied keys.",
      "date_discovered": "2026-03-18",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/agent-activity.ts",
        "app/api/chat/route.ts",
        "app/api/agent-hub/sessions/route.ts"
      ]
    },
    {
      "id": "FAIL-063",
      "category": "security",
      "title": "Path traversal via unsanitized agentId in file-based JSON storage",
      "symptoms": "Attacker sends crafted agentId like \"../../etc/passwd\" or \"__proto__\" as JSON key, potentially causing prototype pollution or writing arbitrary keys into the activity file",
      "root_cause": "recordAgentActivity(agentId) accepts raw string from request body and uses it directly as a JSON object key without validation. The agentId flows from POST /api/chat body → recordAgentActivity → map[agentId] = timestamp. No allowlist or format check exists.",
      "solution": "Validate agentId against a strict regex (e.g. /^[a-zA-Z0-9_-]{1,64}$/) or check against known agent list before using as object key. Also reject __proto__, constructor, prototype keys to prevent prototype pollution.",
      "date_discovered": "2026-03-18",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/agent-activity.ts",
        "app/api/chat/route.ts"
      ]
    },
    {
      "id": "FAIL-064",
      "category": "security",
      "title": "X-Forwarded-For trusted without validation in audit log IP extraction",
      "symptoms": "Audit log stores arbitrary attacker-controlled IP addresses; forensic trail is unreliable; 152-FZ compliance audit finds tampered PII (IP is personal data under 152-FZ Art.3)",
      "root_cause": "extractClientIP() splits X-Forwarded-For and takes leftmost value without validating it is a valid IP or that the proxy chain is trusted. Any client can prepend a fake IP.",
      "solution": "1) Validate extracted IP with net.isIP() before storing. 2) Configure explicit trusted proxy count (e.g., 1 for Vercel) and read from right side of X-Forwarded-For chain minus trusted hops. 3) Store raw header alongside parsed IP for forensic comparison.",
      "date_discovered": "2026-03-18",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/middleware/extractClientIP.ts",
        "docs/architecture/adr-016-audit-log-last-login.md"
      ]
    },
    {
      "id": "FAIL-065",
      "category": "security",
      "title": "audit_log append-only design blocks 152-FZ Art.14 deletion compliance",
      "symptoms": "User requests data deletion under 152-FZ Art.14. app_user role has REVOKE DELETE. Archival cron sets archived=true but never hard-deletes. PII (ip_address, user_id) persists indefinitely.",
      "root_cause": "Schema designed for tamper-proof audit trail (append-only) without a separate superuser deletion pipeline for GDPR/152-FZ compliance. No hard-DELETE cron or admin endpoint exists.",
      "solution": "Add a superuser-owned pg_cron job that hard-DELETEs rows WHERE archived = true AND occurred_at < NOW() - INTERVAL '3 years'. Create a separate superuser-only function for on-demand deletion by user_id to handle Art.14 requests. Document this in the data processing policy.",
      "date_discovered": "2026-03-18",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "docs/architecture/schema-audit-log.sql",
        "migrations/20240601_001_create_audit_log.sql"
      ]
    },
    {
      "id": "FAIL-066",
      "category": "security",
      "title": "audit_log lacks 152-FZ deletion pipeline and has IP spoofing + tenant isolation risks",
      "symptoms": "1) No DELETE path for user PII in append-only audit_log — 152-FZ Art.21 violation. 2) X-Forwarded-For leftmost IP trusted without proxy validation — audit trail poisoning. 3) tenant_id from JWT claim used directly in SET LOCAL without server-side verification — cross-tenant data leak possible.",
      "root_cause": "ADR-016 focused on performance (BIGSERIAL, lateral JOIN) but omitted: (a) PII deletion mechanism beyond archival, (b) IP validation/sanitization in extractClientIP middleware, (c) server-side tenant_id verification before setting RLS context variable.",
      "solution": "1) Create SECURITY DEFINER function with dedicated data_deletion_officer role for hard-delete of user PII. 2) Read rightmost X-Forwarded-For hop (proxy-set) instead of leftmost (client-set), validate as IP. 3) Resolve tenant_id from users table via jwt.sub, never trust JWT tenant_id claim directly.",
      "date_discovered": "2026-03-18",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "docs/architecture/adr-016-audit-log-last-login.md",
        "docs/architecture/schema-audit-log.sql",
        "lib/middleware/extractClientIP",
        "lib/middleware/setTenantContext"
      ]
    },
    {
      "id": "FAIL-068",
      "category": "security",
      "title": "Admin PII endpoint missing rate-limit, cache headers, and log sanitization",
      "symptoms": "GET /api/v1/admin/users exposes admin emails, names, and activity timestamps without rate limiting — enabling account enumeration and active-admin fingerprinting. Fire-and-forget log writes adminId to file-based data/logs.json which may not reside on Timeweb VPS. No Cache-Control headers prevent browser/CDN caching of PII response.",
      "root_cause": "No rate-limit middleware exists in app/api/. Log storage (lib/logs-storage.ts) writes to local filesystem data/logs.json — location depends on deployment host, not guaranteed Timeweb. API contract specifies 30 req/min but no enforcement mechanism exists in codebase. No response header policy for admin routes.",
      "solution": "1) Add rate-limit middleware in app/api/admin/users/route.ts using Map-based sliding window (no new deps) — 30 req/min keyed by JWT sub claim. 2) In same route file, set headers: Cache-Control: no-store, no-cache; Pragma: no-cache; X-Content-Type-Options: nosniff. 3) In fire-and-forget log call, replace raw adminId with HMAC-SHA256(adminId, process.env.LOG_HMAC_SECRET) using Node crypto.createHmac — preserves audit correlation without storing reversible PII. 4) Verify data/logs.json path resolves to Timeweb VPS in deployment config.",
      "date_discovered": "2026-03-18",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/admin/users/route.ts",
        "lib/logs-storage.ts",
        "app/api/logs/route.ts"
      ]
    },
    {
      "id": "FAIL-069",
      "category": "security",
      "title": "PII leakage via file-based logs — adminId written to data/logs.json outside Timeweb PG",
      "symptoms": "Fire-and-forget log spec writes adminId (PII-adjacent UUID linked to a physical person) into local file data/logs.json via addLog(). This file resides on the Next.js deployment host, NOT Timeweb PostgreSQL. If deployed to Vercel/non-Russian hosting, this violates 152-FZ Art.18 data residency.",
      "root_cause": "addLog() in lib/logs-storage.ts writes to a local JSON file (line 7: data/logs.json). The ADR-011 fire-and-forget log includes metadata.adminId. No check ensures this file is on a Russian-jurisdiction server. No PII scrubbing before write.",
      "solution": "1) Strip adminId from fire-and-forget log metadata — log only action type and timestamp, no identifiers. OR 2) Confirm deployment target is Timeweb VPS (Russian jurisdiction) and document in ADR. 3) Backend-Agent must ensure metadata field contains NO PII (no adminId, no email, no name). QA-Agent must add assertion: grep data/logs.json for UUID patterns → fail if found.",
      "date_discovered": "2026-03-18",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/logs-storage.ts",
        "app/api/logs/route.ts",
        "app/api/admin/users/route.ts"
      ]
    },
    {
      "id": "FAIL-070",
      "category": "security",
      "title": "No auth on POST /api/pipeline/analytics — unauthenticated data injection",
      "symptoms": "POST /api/pipeline/analytics accepts arbitrary JSON without session/auth check, allowing anyone to inject fake pipeline run data into the analytics store.",
      "root_cause": "Route handler at app/api/pipeline/analytics/route.ts lines 8-18 calls savePipelineRun(execution) directly from request.json() with zero authentication or authorization checks.",
      "solution": "Add session auth check as the FIRST operation in both POST and GET handlers of /api/pipeline/analytics/route.ts, consistent with FAIL-025 pattern. The new /api/pipeline/summary route must also enforce this (ADR already specifies it).",
      "date_discovered": "2026-03-19",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/pipeline/analytics/route.ts"
      ]
    },
    {
      "id": "FAIL-071",
      "category": "security",
      "title": "Sibling route /api/pipeline/analytics has no auth — pattern must not propagate to /summary",
      "symptoms": "GET /api/pipeline/analytics and POST /api/pipeline/analytics accept requests without any session/auth check. Any unauthenticated user can read agent learning context or write pipeline runs.",
      "root_cause": "Route handler at app/api/pipeline/analytics/route.ts (lines 8-37) never calls getServerSession or any auth middleware before processing requests.",
      "solution": "Add session auth check as the FIRST operation in both GET and POST handlers per FAIL-025 pattern. The new /api/pipeline/summary route MUST include auth before any fs.readFile or fs.stat call. Backport auth to the existing /analytics route as well.",
      "date_discovered": "2026-03-19",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/pipeline/analytics/route.ts",
        "app/api/pipeline/summary/route.ts"
      ]
    },
    {
      "id": "FAIL-072",
      "category": "bad-code",
      "title": "StageStatusBadge missing pill/badge wrapper — renders as bare inline text, not a styled badge",
      "symptoms": "StageStatusBadge renders only an icon + optional text with `inline-flex items-center gap-1.5 text-xs font-medium` — no background, no border, no border-radius, no padding. Design spec requires pill shape (rounded-full, px-2 py-0.5, bg-*-50, border). On mobile 375px the status is nearly invisible against white card backgrounds.",
      "root_cause": "The component applies only `config.color` (a text-color class) from STAGE_STATUS_CONFIG but never applies background, border, or padding classes. The design spec Tailwind classes (e.g. `bg-blue-50 border border-blue-500 rounded-full px-2 py-0.5`) are defined in the design doc but were not implemented in the component.",
      "solution": "Add `rounded-full px-2 py-0.5` base classes to the `<span>` and include `config.bg` and `config.border` fields in STAGE_STATUS_CONFIG. Each status entry must carry its full pill styling, not just text color. Example: `className={cn(\"inline-flex items-center gap-1.5 rounded-full px-2 py-0.5 text-[11px] font-mono\", config.bg, config.border, config.color)}`",
      "date_discovered": "2026-03-19",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "components/orchestration/stage-status-badge.tsx",
        "lib/constants/stage-status.ts"
      ]
    },
    {
      "id": "FAIL-073",
      "category": "bad-code",
      "title": "routeTask() and executePipeline() have no AbortController — requests hang on navigation away",
      "symptoms": "When user navigates away from /orchestration while routing or executing, the fetch calls in routeTask() (POST /api/pipeline/route) and executePipeline() continue running. State setters (setIsRouting, setActiveExecution) are called on unmounted component, causing React warnings and potential stale-state bugs in the next page load.",
      "root_cause": "Neither routeTask() nor the executePipeline() wrapper use AbortController or check a mounted-ref before calling setState. The page.tsx has no cleanup in useEffect for these async operations.",
      "solution": "Add `const abortRef = useRef(new AbortController())` and pass `signal: abortRef.current.signal` to all fetch calls. In a useEffect cleanup: `return () => abortRef.current.abort()`. Alternatively wrap in try/catch that ignores AbortError.",
      "date_discovered": "2026-03-19",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/(shell)/orchestration/page.tsx"
      ]
    },
    {
      "id": "FAIL-074",
      "category": "bad-code",
      "title": "InsertStepButton position calculation off-by-one for parallel groups",
      "symptoms": "After a parallel group, InsertStepButton receives a position that equals the last step index of the group rather than the index AFTER the group. New steps inserted after a parallel group land inside it instead of after it.",
      "root_cause": "getInsertPositionForGroup accumulates steps[0..groupIndex-1] but does NOT include the current group's own steps when called with gi+1. For a parallel group of N steps the returned position is the start index of that group, not start+N.",
      "solution": "Change the loop bound: `for (let g = 0; g <= groupIndex && g < groups.length; g++)` — include the current group so the returned position is the index AFTER all its steps. Add a unit test covering a 3-step parallel group followed by a single step.",
      "date_discovered": "2026-03-19",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "components/orchestration/pipeline-graph.tsx"
      ]
    },
    {
      "id": "FAIL-075",
      "category": "bad-code",
      "title": "MobileNav conditionally returns null — no CSS transition on close, causes layout flash",
      "symptoms": "When isOpen becomes false, the component immediately returns null without playing the close animation. The drawer disappears instantly (no slide-out), causing a jarring visual flash instead of the 250ms ease-in close animation specified in the design spec.",
      "root_cause": "mobile-nav.tsx line 29: `if (!isOpen) return null` — the component is unmounted immediately when isOpen=false. CSS animations (animate-in slide-in-from-left) only play on mount, not on unmount. There is no exit animation mechanism.",
      "solution": "Replace conditional null return with a visibility/transform approach: keep the component mounted, use CSS `translate-x-[-100%]` when closed and `translate-x-0` when open. Or use a library like framer-motion AnimatePresence. At minimum, add a `data-state` attribute and CSS keyframe for slide-out. The backdrop also disappears instantly.",
      "date_discovered": "2026-03-19",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "components/shell/mobile-nav.tsx"
      ]
    },
    {
      "id": "FAIL-076",
      "category": "security",
      "title": "Loyalty ledger CSV export endpoint lacks authorization scoping — full PII exfiltration risk",
      "symptoms": "GET /api/loyalty/clients/:client_id/ledger/export returns full unpaginated ledger as CSV with no salon_id scoping check. Any authenticated user with a valid JWT can export any client's complete financial loyalty history by enumerating client_id UUIDs. The dashboard endpoint also leaks client names in top_clients without verifying the requester owns that salon.",
      "root_cause": "API contract defines auth as \"JWT required (admin)\" but does not specify salon-level tenant isolation. No middleware enforces that the authenticated admin belongs to the salon owning the client_id. The export endpoint has no pagination and streams full data, amplifying the impact of any access control bypass. Combined with 152-FZ PII requirements, this is a data breach vector.",
      "solution": "Every endpoint accepting client_id or salon_id must verify the authenticated user's salon_id matches the resource's salon_id via middleware (tenant isolation guard). The export endpoint must additionally enforce row-level security or an application-layer check that client_id belongs to the admin's salon. Add a shared `assertSalonOwnership(jwt, client_id)` helper used by all route handlers. Rate limit the export endpoint more aggressively (current 10/min is too high for full-dump endpoints).",
      "date_discovered": "2026-03-22",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/loyalty/clients/[client_id]/ledger/export/route.ts",
        "app/api/loyalty/clients/[client_id]/balance/route.ts",
        "app/api/loyalty/clients/[client_id]/ledger/route.ts",
        "app/api/loyalty/clients/[client_id]/manual-adjust/route.ts",
        "app/api/loyalty/dashboard/route.ts"
      ]
    },
    {
      "id": "FAIL-077",
      "category": "security",
      "title": "Loyalty domain lacks tenant isolation — no BOLA protection on client_id paths",
      "symptoms": "All 6 endpoints under /api/loyalty/clients/:client_id/* accept arbitrary client_id in the URL path. No middleware validates that the authenticated salon/user owns the requested client_id. An attacker with a valid JWT for Salon A can read balances, export ledgers, and manually adjust points for clients belonging to Salon B by enumerating client_id UUIDs.",
      "root_cause": "ADR-003 specifies JWT auth on every endpoint but defines no authorization middleware that joins client_id → salon_id and compares against the JWT's salon claim. The API contract lists 'Auth: JWT required' without specifying tenant-scoping logic. loyalty_balance has a salon_id column but no endpoint contract mandates a WHERE salon_id = jwt.salon_id guard.",
      "solution": "Create a reusable middleware (e.g., assertClientBelongsToSalon) that performs SELECT salon_id FROM loyalty_balance WHERE client_id = :client_id and compares against jwt.salon_id, returning 403 on mismatch. Apply to ALL /clients/:client_id/* routes. Add integration tests with cross-tenant client_ids. Also add salon_id WHERE clause to every loyalty_ledger and loyalty_balance query as a defense-in-depth measure.",
      "date_discovered": "2026-03-22",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/loyalty/clients/[client_id]/balance/route.ts",
        "app/api/loyalty/clients/[client_id]/ledger/route.ts",
        "app/api/loyalty/clients/[client_id]/ledger/export/route.ts",
        "app/api/loyalty/clients/[client_id]/manual-adjust/route.ts",
        "lib/loyalty/balance.service.ts",
        "lib/loyalty/ledger.service.ts"
      ]
    },
    {
      "id": "FAIL-078",
      "category": "security",
      "title": "Internal API secret in header is insufficient for server-to-server auth on earn/reverse/bonus endpoints",
      "symptoms": "POST /api/loyalty/ledger/earn, /reverse, /bonus/* rely solely on x-internal-secret header matching process.env.INTERNAL_API_SECRET. If the secret leaks (logs, error traces, env var exposure), any external actor can award arbitrary points to any clientId, bypassing all role checks.",
      "root_cause": "Shared-secret header auth has no per-request binding — it is replayable, loggable, and has no expiry. A single leak grants permanent unrestricted access to point-crediting endpoints. There is no IP allowlist, no request signing, and no nonce/timestamp to prevent replay attacks.",
      "solution": "Add HMAC-SHA256 request signing: sign (method + path + body-hash + unix-timestamp) with INTERNAL_API_SECRET; include signature + timestamp in headers; reject requests where timestamp is >30s old. Alternatively, restrict these routes to localhost/loopback only at the network layer (Next.js middleware or reverse proxy), so they are never reachable from the public internet regardless of secret exposure.",
      "date_discovered": "2026-03-22",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/loyalty/ledger/earn/route.ts",
        "app/api/loyalty/ledger/reverse/route.ts",
        "app/api/loyalty/bonus/birthday/route.ts",
        "app/api/loyalty/bonus/referral/route.ts",
        "lib/loyalty/rate-limit.ts"
      ]
    },
    {
      "id": "FAIL-079",
      "category": "security",
      "title": "SQL Injection via unparameterized `type` filter in getLedgerEntries()",
      "symptoms": "In lib/loyalty/ledger.service.ts line 50, the `type` query parameter is interpolated directly into the SQL string: `AND type = '${type}'`. Any caller who passes a crafted `type` value (e.g., `' OR '1'='1`) can read arbitrary rows across all salons, bypassing tenant isolation and BOLA guards entirely.",
      "root_cause": "The developer used a JavaScript template literal to build the WHERE clause instead of a parameterized placeholder ($N). The `type` value comes from the HTTP query string and is never validated or escaped before interpolation.",
      "solution": "Replace the interpolated typeFilter with a parameterized approach: collect params in an array and append `AND type = $N` only when type is provided. Additionally, validate `type` against the allowed enum ('earn','redeem','manual_adjust','expire','reverse','bonus') with Zod before it reaches the service layer.",
      "date_discovered": "2026-03-22",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/loyalty/ledger.service.ts"
      ]
    },
    {
      "id": "FAIL-080",
      "category": "security",
      "title": "SQL Injection via string interpolation in getLedgerEntries type filter",
      "symptoms": "getLedgerEntries() in lib/loyalty/ledger.service.ts line 50 interpolates the `type` query parameter directly into the SQL string: `AND type = '${type}'`. Any authenticated user can inject arbitrary SQL through the ledger type filter.",
      "root_cause": "Developer used template literal string interpolation instead of a parameterized placeholder for the optional `type` filter, bypassing pg's parameterized query protection. The pattern was likely copied from a non-security-sensitive context.",
      "solution": "Add `type` as a positional parameter ($4). Use: `AND ($4::text IS NULL OR type = $4)` with `type ?? null` in the values array. Apply the same fix to the COUNT query. Never interpolate user-controlled values into SQL strings regardless of upstream validation.",
      "date_discovered": "2026-03-22",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/loyalty/ledger.service.ts"
      ]
    },
    {
      "id": "FAIL-081",
      "category": "security",
      "title": "SQL Injection via string interpolation in getLedgerEntries type filter",
      "symptoms": "Line 50 of lib/loyalty/ledger.service.ts builds a WHERE clause fragment using `AND type = '${type}'` — direct string interpolation of user-controlled input into raw SQL. An authenticated attacker can inject arbitrary SQL via the `type` query parameter on GET /api/loyalty/clients/:clientId/ledger, enabling full table exfiltration, data corruption, or privilege escalation within the loyalty_ledger table (contains PII).",
      "root_cause": "The `type` parameter from the HTTP query string is interpolated directly into the SQL string on line 50 instead of being passed as a parameterized placeholder ($N). The function uses parameterized queries for `client_id`, `limit`, and `offset` ($1–$3) but bypasses this pattern for the optional `type` filter by building the clause via template literal.",
      "solution": "Replace the interpolated filter with parameterized placeholders. Push `type` into the values array as $4 and rewrite the WHERE clause to: `AND ($4::text IS NULL OR type = $4)`. Pass `type ?? null` as the 4th parameter in both the SELECT and COUNT queries. Additionally, validate `type` against the LedgerType enum at the service boundary (Zod schema in the route handler) BEFORE it reaches the query — defense in depth. Never use string interpolation for any SQL fragment derived from user input anywhere in the loyalty module.",
      "date_discovered": "2026-03-22",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "lib/loyalty/ledger.service.ts"
      ]
    },
    {
      "id": "FAIL-082",
      "category": "security",
      "title": "Missing route handler layer — zero server-side enforcement for loyalty API",
      "symptoms": "The directory app/api/loyalty/ does not exist on disk. All 12 API routes defined in the ADR contract (config CRUD, accounts, ledger, earn, redeem, export) have no server-side implementation, meaning there is zero enforcement of JWT auth, BOLA guards, role checks, rate limits, input validation, or the redemption_enabled block at the HTTP boundary. Any future scaffolding without a security checklist will expose PII and allow unauthenticated point manipulation.",
      "root_cause": "Route handlers were never created — the service layer (lib/loyalty/*.service.ts) was built first without corresponding Next.js API route files. There is no architectural gate or pre-merge checklist requiring security controls before a route handler can be merged.",
      "solution": "Every route handler in app/api/loyalty/ MUST enforce before merge: (1) JWT verification middleware at route level extracting salon_id from token claims; (2) BOLA guard on all :clientId routes verifying client.salon_id === jwt.salon_id; (3) Zod input validation on all POST/PUT bodies and query params; (4) Role-based access control per ADR spec; (5) Rate limiting per endpoint; (6) x-internal-secret routes must use crypto.timingSafeEqual for constant-time comparison; (7) Redeem route must check redemption_enabled server-side AND hard-return 403 with code redemption_blocked_ra1 regardless of config value until RA-1 is resolved; (8) CSV export must set Cache-Control: no-store and sanitize fields against formula injection.",
      "date_discovered": "2026-03-22",
      "recurrence": "Discovered by QA-Agent",
      "source": "qa-agent",
      "affected_files": [
        "app/api/loyalty"
      ]
    }
  ]
}